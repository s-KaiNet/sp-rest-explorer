---
phase: 29-automated-github-pages-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - docs/
autonomous: true
requirements:
  - REPO-01

must_haves:
  truths:
    - "The docs/ folder no longer exists in the repository"
    - "Git history shows docs/ was intentionally removed"
  artifacts:
    - path: "docs/"
      provides: "MUST NOT EXIST — this artifact is deleted, not created"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "app/dist/"
      via: "upload-pages-artifact reads from app/dist/ (no dependency on docs/)"
      pattern: "path.*app/dist"
---

<objective>
Delete the committed docs/ folder from the repository, completing the migration from committed build output to CI/CD-managed deployment.

Purpose: This is Step 2 of the two-step cutover. Plan 01 verified that automated deployment works, so the docs/ fallback is no longer needed. Removing it ensures the repository no longer carries build artifacts.
Output: Repository with no docs/ directory.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-automated-github-pages-deployment/29-CONTEXT.md
@.planning/phases/29-automated-github-pages-deployment/29-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the committed docs/ directory</name>
  <files>docs/</files>
  <action>
Remove the entire `docs/` directory from the repository using `git rm -r docs/`.

This directory currently contains the committed build output (index.html, favicon files, assets/ with JS/CSS bundles). It is no longer needed because:
- Plan 01 switched Vite output to `app/dist/`
- Plan 01 added a GitHub Actions workflow that builds and deploys from `app/dist/`
- Plan 01's checkpoint verified the automated deploy works
- Root `.gitignore` already blocks `docs/` from being re-created

The git rm command stages the deletion. The executor should commit this as a separate, clean commit with a message like: `chore(29-02): remove committed docs/ build output`

After deletion, verify that `docs/` no longer exists on disk and is not in the git index.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); if(fs.existsSync('docs')){console.error('docs/ still exists');process.exit(1)}else{console.log('docs/ successfully removed')}"</automated>
  </verify>
  <done>The docs/ directory no longer exists in the repository. The repository is clean of committed build artifacts.</done>
</task>

</tasks>

<verification>
- `docs/` directory does not exist on disk
- `git status` shows docs/ removal is committed (clean working tree)
- Root `.gitignore` still contains `docs/` entry (from Plan 01)
- The workflow from Plan 01 still references `app/dist/` (no broken references)
</verification>

<success_criteria>
- docs/ folder is completely removed from the repository
- No build artifacts remain committed in the repo
- Automated deployment (from Plan 01) continues to work since it uses app/dist/
</success_criteria>

<output>
After completion, create `.planning/phases/29-automated-github-pages-deployment/29-02-SUMMARY.md`
</output>
