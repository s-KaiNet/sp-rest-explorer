---
phase: 05-entity-function-detail
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/pages/ExplorePage.tsx
  - app/src/pages/TypesPage.tsx
autonomous: true

must_haves:
  truths:
    - "Function detail in ExplorePage shows entity type links as clickable green links"
    - "Function detail shows COMPOSABLE badge when applicable"
    - "Function parameters display one-per-line as paramName: ParamType with this filtered out"
    - "void return type shows as italic muted text"
    - "No parameters (or only this) shows italic muted none text"
    - "Explore Types welcome screen shows stats (entity count, property count, method count)"
    - "Explore Types welcome screen matches mockup types-welcome design"
  artifacts:
    - path: "app/src/pages/ExplorePage.tsx"
      provides: "Enhanced function detail view with clickable type links"
    - path: "app/src/pages/TypesPage.tsx"
      provides: "Explore Types welcome landing screen with stats"
  key_links:
    - from: "app/src/pages/ExplorePage.tsx"
      to: "react-router Link"
      via: "Entity type links in function parameters and return type"
      pattern: "Link.*entity/"
    - from: "app/src/pages/TypesPage.tsx"
      to: "app/src/lib/metadata"
      via: "useMetadataSnapshot for entity/property/method counts"
      pattern: "useMetadataSnapshot"
---

<objective>
Enhance the existing function detail display in ExplorePage with clickable entity type links, proper void/none handling, and COMPOSABLE badges. Build the Explore Types welcome landing screen with real metadata stats.

Purpose: Function detail improvements satisfy FUNC-01/02/03. The Explore Types welcome screen is the landing page for `/#/entity` before a type is selected.
Output: Enhanced function view in ExplorePage, complete Explore Types welcome screen.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-entity-function-detail/05-CONTEXT.md

## Mockup Reference
@.planning/phases/1-rebuild-ui/mockups/types-view.html

## Key Source Files
@app/src/pages/ExplorePage.tsx
@app/src/pages/TypesPage.tsx
@app/src/hooks/use-api-navigation.ts
@app/src/lib/metadata/types.ts
@app/src/lib/metadata/index.ts
@app/src/routes.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhanced function detail in ExplorePage</name>
  <files>app/src/pages/ExplorePage.tsx</files>
  <action>
    Enhance the existing function detail display in ExplorePage (the `currentFunction` branch in the content area, lines ~132-164). Per user decisions and requirements FUNC-01/02/03:

    **Function name + COMPOSABLE badge** (already working, keep as-is but refine):
    - Function name in blue monospace (`font-mono text-xl font-semibold text-type-fn`).
    - COMPOSABLE badge: small blue pill (`bg-type-fn/10 text-type-fn`). Already implemented — keep.

    **Parameters display** — Replace current parameter rendering with enhanced version:
    - Filter out `this` parameter from display (per user decision — already in current code: check `p.name !== 'this'` but apply explicitly).
    - Display remaining params one-per-line as `paramName: ParamType`.
    - For each parameter type, determine if it's an entity type or primitive:
      - Entity types (not starting with `Edm.`): render as clickable green Link to `/#/entity/{typeName}`. Use React Router `Link` component.
      - Primitive types (`Edm.*`): render as muted monospace text, not clickable.
      - Collection types: parse with regex `/^Collection\((.+)\)$/`. If inner type is entity: render "Collection(" as muted text + inner type as green Link + ")" as muted text. If inner type is Edm.*: render entire string as muted text.
    - When no params after filtering `this`: show italic muted "none" text per mockup (`.no-params`).
    - Param name: foreground font-medium mono. Separator ": " in muted. Type: colored per above.

    **Return type display** — Replace current return type rendering:
    - Same entity/primitive/Collection logic as parameters.
    - `void` return type (or empty/undefined returnType): italic muted text saying "void" per mockup.
    - If function is composable: show COMPOSABLE badge next to return type (already working, keep).

    **Do NOT** create a separate TypeLink component here — just inline the link logic. Plan 03 will refactor to use the shared TypeLink when wiring EntityDetail. This avoids a cross-plan dependency in Wave 1.

    Keep all other ExplorePage logic unchanged (sidebar, breadcrumb, filter, root welcome, recently visited).
  </action>
  <verify>
    `npm run build` in app/ directory succeeds. Navigate to `/#/_api/web/GetChanges` in the running app — should see function name in blue, parameter `query: SP.ChangeQuery` with SP.ChangeQuery as a green clickable link, return type `Collection(SP.Change)` with COMPOSABLE badge. Navigate to a function with no params (e.g., `/#/_api/web/Lists/GetById` → then a void-returning method) — should see "none" for params.
  </verify>
  <done>
    Function detail in ExplorePage shows parameters one-per-line with entity types as clickable green links navigating to `/#/entity/{typeName}`. Primitive types are gray/muted. void returns show in italic muted text. "none" shows when no parameters after filtering `this`. COMPOSABLE badge displays for composable functions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Explore Types welcome landing screen</name>
  <files>app/src/pages/TypesPage.tsx</files>
  <action>
    Replace the "Coming soon" placeholder in TypesPage with the full Explore Types welcome landing screen per the mockup `.types-welcome` design and user decisions:

    **Route handling**: TypesPage handles both `/#/entity` (no type selected → welcome screen) and `/#/entity/:typeName` (type selected → will be wired in Plan 03). For now, implement welcome screen only. Use `useParams()` to check for `typeName` — if absent, show welcome; if present, show "Loading..." placeholder (Plan 03 adds EntityDetail).

    **Welcome screen layout** (per mockup `.types-welcome`):
    - Centered vertically and horizontally in content area, max-width ~420px text.
    - Green icon box at top: 72x72px rounded-2xl with green-light bg, large "T" in entity-green, bold.
    - Title: "Explore Types" (20px bold).
    - Description: "Browse all entity types defined in the SharePoint REST API metadata. Select a type from the sidebar to view its properties, navigation properties, and methods." (14px secondary text, relaxed line height).
    - **Stats row** with real counts from metadata:
      - Entity Types count: count of `Object.keys(metadata.entities)`
      - Properties count: sum of all entities' `properties.length`
      - Methods count: sum of all entities' `functionIds.length`
      - Each stat: large number (28px bold entity-green) + uppercase label (12px muted).
      - Use `useMetadataSnapshot()` and `useMemo` to compute counts.
    - **Hint box** at bottom: green-light bg rounded, with cursor icon + text "Select a type from the sidebar, or use Ctrl K to search" with kbd-styled shortcut key.

    Per user decision: Explore Types sidebar with full entity list is deferred to v2. For now, TypesPage has no sidebar — just the full-width content area.

    **Note:** No sidebar for Explore Types page in v1 (deferred). The page sits in the main content area without sidebar layout.
  </action>
  <verify>
    `npm run build` in app/ directory succeeds. Navigate to `/#/entity` in the running app — should see centered welcome screen with "Explore Types" title, real metadata stats (2,449+ entities, 11,000+ properties, 3,500+ methods), and hint box. Navigate to `/#/entity/SP.List` — should show placeholder (not welcome screen).
  </verify>
  <done>
    Explore Types welcome screen at `/#/entity` shows centered layout with green T icon, title, description, three real stat counters from metadata, and Ctrl+K hint. Route `/#/entity/:typeName` shows placeholder ready for Plan 03.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- Function detail in ExplorePage: entity type links are clickable green, void is italic muted, none for empty params, COMPOSABLE badge shown
- Explore Types welcome screen: real stats computed from metadata, layout matches mockup
- No regressions in existing navigation, sidebar, or breadcrumb behavior
</verification>

<success_criteria>
1. Entity types in function parameters and return types are clickable green links to `/#/entity/{typeName}`
2. Primitive types (Edm.*) are gray muted text, not clickable
3. Collection types with entity inner types have the entity portion clickable
4. void return type shows as italic muted, "none" for no params
5. `this` parameter is filtered from display
6. Explore Types welcome screen shows at `/#/entity` with real metadata stats
7. Zero TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-entity-function-detail/05-02-SUMMARY.md`
</output>
