---
phase: 23-recently-visited-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/stores/recently-visited-store.ts
  - app/src/hooks/use-recently-visited.ts
  - app/src/hooks/index.ts
  - app/src/components/search/CommandPalette.tsx
  - app/src/App.tsx
  - app/src/pages/ExplorePage.tsx
  - app/src/pages/TypesPage.tsx
  - app/src/pages/HomePage.tsx
autonomous: false
requirements:
  - RVIS-01
  - RVIS-02
  - RVIS-03
  - RVIS-04
  - RVIS-05

must_haves:
  truths:
    - "Clicking Clear on the home page removes all recently visited entries from every page — they never reappear without new navigation"
    - "Selecting an entity from search records kind 'entity' and shows the Braces (orange) icon in recently visited"
    - "Selecting an endpoint from search records the actual kind (function/navProperty/root) and shows the matching icon"
    - "All recently visited consumers share one Zustand store instance — no independent useState for this state"
    - "Old localStorage entries with buggy kinds are purged on store version upgrade"
  artifacts:
    - path: "app/src/stores/recently-visited-store.ts"
      provides: "Zustand store with persist middleware for recently visited items"
      contains: "create.*persist"
    - path: "app/src/hooks/use-recently-visited.ts"
      provides: "Thin wrapper hook around Zustand store for API compatibility"
      contains: "useRecentlyVisitedStore"
    - path: "app/src/components/search/CommandPalette.tsx"
      provides: "Expanded SearchSelection with granular kind field"
      contains: "'entity' | 'function' | 'navProperty' | 'root'"
  key_links:
    - from: "app/src/components/search/CommandPalette.tsx"
      to: "app/src/App.tsx"
      via: "SearchSelection.kind passed to onSelect → addVisit"
      pattern: "kind:.*function|navProperty|root|entity"
    - from: "app/src/App.tsx"
      to: "app/src/stores/recently-visited-store.ts"
      via: "addVisit passes SearchSelection.kind straight through"
      pattern: "addVisit.*kind.*selection\\.kind"
    - from: "app/src/pages/HomePage.tsx"
      to: "app/src/stores/recently-visited-store.ts"
      via: "clearAll triggers atomic state reset across all consumers"
      pattern: "clearAll"
---

<objective>
Fix three recently visited bugs by migrating state to a Zustand store and expanding the SearchSelection type with granular kind values.

Purpose: The "clear" button doesn't work across components (stale useState), entity types from search get the wrong icon (entity→root remapping), and endpoint icons always show function (kindMap loses navProperty). All three share the root cause: independent useState hooks and a lossy kind mapping in App.tsx.

Output: Working recently visited feature with correct icons, atomic clear, and shared Zustand state.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-recently-visited-fix/23-CONTEXT.md
@app/src/hooks/use-recently-visited.ts
@app/src/stores/app-store.ts
@app/src/components/search/CommandPalette.tsx
@app/src/App.tsx
@app/src/pages/ExplorePage.tsx
@app/src/pages/TypesPage.tsx
@app/src/pages/HomePage.tsx
@app/src/hooks/index.ts
@app/src/lib/api-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Zustand recently-visited store and update SearchSelection type</name>
  <files>
    app/src/stores/recently-visited-store.ts
    app/src/hooks/use-recently-visited.ts
    app/src/hooks/index.ts
    app/src/components/search/CommandPalette.tsx
  </files>
  <action>
**1. Create `app/src/stores/recently-visited-store.ts`:**

Create a new Zustand store following the pattern in `app/src/stores/app-store.ts` (uses `create` from 'zustand').

Store shape:
```typescript
interface RecentlyVisitedState {
  items: RecentlyVisitedItem[]
  addVisit: (item: Omit<RecentlyVisitedItem, 'timestamp'>) => void
  clearAll: () => void
}
```

Use Zustand `persist` middleware with:
- `name: 'recently-visited'` (same localStorage key as current)
- `version: 2` — version bump triggers migration that returns `{ items: [] }` to clear buggy old entries. This is intentional per user decision: "Clear old localStorage data on store upgrade — fresh start, no migration of buggy entries"
- `migrate: () => ({ items: [] })` — any version < 2 gets wiped

The `addVisit` logic is the same as current hook: dedup by path, prepend with timestamp, slice to MAX_ITEMS (12). The `clearAll` sets items to `[]`.

Import `RecentlyVisitedItem` type from the hook file (or co-locate the type in this new store file — use your judgment per Claude's Discretion).

**2. Update `app/src/hooks/use-recently-visited.ts`:**

Convert to a thin wrapper around the Zustand store:
- Import `useRecentlyVisitedStore` from `@/stores/recently-visited-store`
- Remove `useState`, `useCallback`, `loadFromStorage`, `saveToStorage` helpers
- Keep the `RecentlyVisitedItem` interface export (for backward compatibility with HomePage import)
- `useRecentlyVisited()` returns `{ items, addVisit, clearAll }` by selecting from the store
- Use Zustand selectors to pick `items`, `addVisit`, `clearAll` — either individual scalar selectors or `useShallow` (per STATE.md warning about Zustand v5 selector instability, prefer `useShallow` for the object return)

**3. Expand `SearchSelection` type in `app/src/components/search/CommandPalette.tsx`:**

Change the `SearchSelection` interface:
```typescript
// BEFORE
kind: 'entity' | 'endpoint'

// AFTER
kind: 'entity' | 'function' | 'navProperty' | 'root'
```

Update `handleEntitySelect` (line ~314): keep `kind: 'entity'` — no change needed.

Update `handleEndpointSelect` (line ~326): Instead of always emitting `kind: 'endpoint'`, compute the actual kind from the search result:
```typescript
const kind: SearchSelection['kind'] = (result.isRoot as boolean)
  ? 'root'
  : (result.endpointKind as string) === 'function'
    ? 'function'
    : 'navProperty'
```
This leverages the `endpointKind` and `isRoot` fields already available on search results — per CONTEXT.md: "The endpointKind is already computed inside CommandPalette but never propagated through SearchSelection."

**4. Update hooks barrel export in `app/src/hooks/index.ts`:**

No changes needed to the exports — `useRecentlyVisited` and `RecentlyVisitedItem` are still exported from the same path. Just verify the barrel still works after the hook refactor.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify no TypeScript errors from the refactored hook, new store, and expanded SearchSelection type</manual>
  </verify>
  <done>
    - Zustand store exists at `app/src/stores/recently-visited-store.ts` with persist middleware (version 2)
    - `useRecentlyVisited` hook is a thin wrapper around the Zustand store
    - `SearchSelection.kind` is `'entity' | 'function' | 'navProperty' | 'root'`
    - `handleEndpointSelect` in CommandPalette emits the correct granular kind
    - TypeScript compiles with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx consumers and remove kindMap remapping</name>
  <files>
    app/src/App.tsx
    app/src/pages/ExplorePage.tsx
    app/src/pages/TypesPage.tsx
    app/src/pages/HomePage.tsx
  </files>
  <action>
**1. Update `app/src/App.tsx` `handleSelect`:**

Remove the `kindMap` object entirely (lines 55-58). Since `SearchSelection.kind` now carries the exact kind needed for recently visited, pass it straight through:

```typescript
const handleSelect = useCallback(
  (selection: SearchSelection) => {
    navigate(selection.path)
    // SearchSelection.kind now matches RecentlyVisitedItem.kind directly — no mapping needed
    addVisit({
      name: selection.name,
      path: selection.path,
      kind: selection.kind,
    })
    setPaletteOpen(false)
  },
  [navigate, addVisit],
)
```

This fixes Bug #2 (entity→root remapping) and Bug #3 (endpoint→function lossy mapping) per user decision: "Remove the kindMap in App.tsx — CommandPalette emits the correct kind, App.tsx passes it straight through to addVisit with no mapping."

**2. Verify `app/src/pages/ExplorePage.tsx` — no changes needed:**

ExplorePage (lines 49-56) already computes kind correctly:
- `segments.length === 2` → `'root'`
- Otherwise uses `last.kind` which is `'function'` | `'navProperty'` from the navigation data

This is already correct. No changes needed.

**3. Verify `app/src/pages/TypesPage.tsx` — no changes needed:**

TypesPage (lines 71-77) already records `kind: 'entity'` when selecting a type. This is correct.

**4. Verify `app/src/pages/HomePage.tsx` — no changes needed to data flow:**

HomePage reads `items` and `clearAll` from `useRecentlyVisited()`. Since the hook now wraps the Zustand store, `clearAll` will atomically clear state for all consumers (fixing Bug #1). The `kindToApiType` mapping on line 27 already handles all 4 kinds correctly: `root→root, function→function, entity→entity, navProperty→nav`. No changes needed.

**Summary of what changes in this task:**
- `App.tsx`: Remove `kindMap`, pass `selection.kind` directly to `addVisit`
- `ExplorePage.tsx`: No changes (already correct)
- `TypesPage.tsx`: No changes (already correct)
- `HomePage.tsx`: No changes (already correct, clearAll now works atomically via shared store)
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify the kindMap is gone from App.tsx and selection.kind is passed directly</manual>
  </verify>
  <done>
    - `kindMap` removed from App.tsx — `selection.kind` passed directly to `addVisit`
    - ExplorePage, TypesPage, HomePage unchanged (already correct)
    - TypeScript compiles with no errors
    - All 3 bugs addressed: atomic clear via Zustand, entity kind preserved, endpoint kinds granular
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all three recently visited bugs are fixed</name>
  <action>Human verification of all three bug fixes in the running app</action>
  <what-built>
    Migrated recently visited from independent useState hooks to shared Zustand store with persist middleware. Expanded SearchSelection with granular kind types. Removed lossy kindMap from App.tsx. Three bugs fixed:
    1. Clear button now purges state atomically across all components
    2. Entity types from search show correct Braces icon (entity kind preserved)
    3. Endpoint icons show correct type (function/navProperty/root) instead of always function
  </what-built>
  <how-to-verify>
    1. Run `cd app && npm run dev` to start the dev server
    2. Open the app in the browser

    **Bug #1 — Clear button:**
    3. Navigate to a few endpoints (e.g. `/_api/web`, `/_api/web/Lists`, `/_api/site`)
    4. Return to the home page — verify recently visited cards appear
    5. Click "Clear" — verify all cards disappear
    6. Navigate to `/_api` (Explore page) — verify no stale entries anywhere
    7. Return to home page — verify still empty (no ghost entries)

    **Bug #2 — Entity icon from search:**
    8. Press Ctrl+K, search for an entity (e.g. "SPWeb")
    9. Select it from the Entities group
    10. Return to home page — verify the recently visited card shows the **Braces icon (orange)**, NOT the Box icon (green)

    **Bug #3 — Endpoint icons from search:**
    11. Press Ctrl+K, search for an endpoint (e.g. "GetByTitle")
    12. Select a function result
    13. Return to home page — verify the card shows the **Zap icon (amber)** for functions
    14. Press Ctrl+K, search for a navigation property endpoint
    15. Select it, return to home page — verify it shows the **Compass icon (blue)** for navProperties
    16. Search for a root endpoint (e.g. "web" in path mode), select it — verify it shows the **Box icon (green)** for root

    **Store upgrade:**
    17. Open DevTools → Application → Local Storage
    18. Verify the `recently-visited` key contains a JSON object with `version: 2` (from persist middleware state wrapper)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd app && npx tsc --noEmit` passes with 0 errors
- Dev server runs: `cd app && npm run dev` starts without errors
- All 3 bugs verified manually via checkpoint
- No regressions in existing recently visited functionality (items still track, persist, show relative times)
</verification>

<success_criteria>
1. Clear button purges recently visited from all consumers atomically
2. Entity types from search display Braces icon in recently visited
3. Endpoint icons in recently visited match their actual type (function/navProperty/root)
4. Single Zustand store shared across App.tsx, ExplorePage, TypesPage, and HomePage
5. Old buggy localStorage entries cleared on version upgrade
6. TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-recently-visited-fix/23-01-SUMMARY.md`
</output>
