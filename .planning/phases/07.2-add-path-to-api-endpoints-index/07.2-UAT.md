---
status: diagnosed
phase: 07.2-add-path-to-api-endpoints-index
source: [07.2-01-SUMMARY.md]
started: 2026-02-14T14:29:00Z
updated: 2026-02-14T15:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Name search preserved
expected: Open the command palette (Ctrl+K). Type "GetByTitle" (a single word, no slash or space). You should see two groups — "Entities" and "API Endpoints" — with matching results. Same behavior as before Phase 07.2.
result: pass

### 2. Path search with slash trigger
expected: In the command palette, type "web/Lists". The search should switch to path mode — only the "API Endpoints" group visible, showing endpoints whose REST paths contain both "web" and "Lists" segments.
result: issue
reported: "when typing web/lists it should search for the EXACT match, ie only api paths, where 'web/lists' is a direct substring"
severity: major

### 3. Path search with space (AND semantics)
expected: In the command palette, type "lists get" (two words separated by space). Path mode activates — only "API Endpoints" group, showing endpoints whose paths contain BOTH "lists" AND "get" segments.
result: issue
reported: "if I use space, I always see no results, no matter which words I use"
severity: major

### 4. _api/ guard — no path mode on bare prefix
expected: In the command palette, type "_api/" alone. This should NOT trigger path mode (it would match all 62K endpoints). You should see normal name-mode results or the default empty state — not a flood of results.
result: pass

### 5. Entities group hidden in path mode
expected: When you type a query with a slash (e.g. "web/"), the "Entities" group should be completely hidden. Only the "API Endpoints" group should be visible.
result: pass

### 6. Path segment highlighting
expected: In path mode (e.g. type "web/Lists"), the matching path segments in each result's path text should be displayed in bold. For example in "_api/web/Lists/GetByTitle", "web" and "Lists" should be bold while other segments stay normal weight.
result: pass

### 7. Placeholder hint for path search
expected: When the command palette opens and the input is empty, the placeholder text should mention path search capability (e.g. "use / for path search" or similar hint).
result: pass

## Summary

total: 7
passed: 5
issues: 2
pending: 0
skipped: 0

## Gaps

- truth: "Path search with slash trigger matches endpoints whose REST paths contain the query as a direct substring"
  status: failed
  reason: "User reported: when typing web/lists it should search for the EXACT match, ie only api paths, where 'web/lists' is a direct substring"
  severity: major
  test: 2
  root_cause: "MiniSearch tokenizes on '/' so 'web/lists' becomes two independent tokens ['web', 'lists'] matched anywhere in the path. User expects contiguous substring matching — 'web/lists' should only match paths containing the literal string 'web/lists'."
  artifacts:
    - path: "app/src/lib/metadata/search-index.ts"
      lines: "61-70"
      issue: "Path index tokenizer splits on '/' destroying adjacency information. MiniSearch is bag-of-tokens and cannot enforce contiguous substring matching."
    - path: "app/src/components/search/CommandPalette.tsx"
      lines: "237-249"
      issue: "Raw query passed to pathIndex.search() — no pre-processing for substring mode"
  missing:
    - "Replace MiniSearch token-based path search with substring filter (path.toLowerCase().includes(query)) for slash-triggered queries"
    - "Update HighlightedPath to highlight the contiguous substring match rather than individual tokens"
  debug_session: ""

- truth: "Space-separated query activates path mode and shows endpoints matching all terms"
  status: failed
  reason: "User reported: if I use space, I always see no results, no matter which words I use"
  severity: major
  test: 3
  root_cause: "Path index tokenizer is text.split('/') — splits only on slash. When user types 'lists get', MiniSearch tokenizes the query with the same splitter, producing ONE token 'lists get' (no slash in query). This single token matches nothing in the index where tokens are individual path segments like 'lists', 'GetById', etc."
  artifacts:
    - path: "app/src/lib/metadata/search-index.ts"
      lines: "61-70"
      issue: "Path tokenizer text.split('/') does not handle space-separated queries. Query 'lists get' becomes single token 'lists get' which matches no indexed token."
    - path: "app/src/lib/metadata/search-index.ts"
      lines: "100-112"
      issue: "detectSearchMode correctly identifies space as path mode, but the path index tokenizer cannot process space-separated input."
    - path: "app/src/components/search/CommandPalette.tsx"
      lines: "80-84"
      issue: "HighlightedPath already splits on /[/ ]+/ (both slash and space) — correct tokenizer exists in display code but not in search index."
  missing:
    - "Either change path tokenizer to split on /[/ ]+/ (both slash and space), or replace space-mode with substring filter like slash-mode"
    - "Ensure query pre-processing is consistent between search and highlighting"
  debug_session: ""
