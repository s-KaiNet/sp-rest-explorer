---
phase: 09-explore-api-sidebar-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [app/src/components/navigation/Sidebar.tsx]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Namespace grouping extracts namespace from entry.name (the item's fully-qualified identifier), not from entry.returnType"
    - "Items under a namespace group show stripped short names (everything after the namespace prefix dot)"
    - "No malformed namespace headers like 'Collection(SP' or 'Edm' appear in the sidebar"
  artifacts:
    - path: "app/src/components/navigation/Sidebar.tsx"
      provides: "Correct namespace extraction from entry.name"
      contains: "getNamespace(entry.name)"
  key_links:
    - from: "app/src/components/navigation/Sidebar.tsx"
      to: "ChildEntry.name"
      via: "getNamespace() parameter"
      pattern: "getNamespace\\(entry\\.name\\)"
---

<objective>
Fix namespace grouping in Explore API sidebar to extract namespace from entry.name instead of entry.returnType.

Purpose: The namespace grouping currently uses entry.returnType (what a function returns) instead of entry.name (the function's fully-qualified identifier). This causes 74% of entries to be grouped under wrong namespaces, with garbage headers like "Collection(SP" and "Edm". The fix matches how Explore Types sidebar correctly groups by entity.fullName.

Output: Corrected Sidebar.tsx with namespace extraction from entry.name
</objective>

<execution_context>
@./.Claude/get-shit-done/workflows/execute-plan.md
@./.Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/namespace-grouping-wrong.md
@app/src/components/navigation/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix getNamespace() to use entry.name instead of entry.returnType</name>
  <files>app/src/components/navigation/Sidebar.tsx</files>
  <action>
Two changes in Sidebar.tsx:

1. **Change getNamespace() signature and JSDoc** (lines 20-30):
   - Change parameter from `returnType?: string` to `name: string` (required, not optional)
   - Update JSDoc comment: "Extract namespace from a fully-qualified name string" (not "returnType string")
   - The function body stays the same (lastIndexOf('.') logic is correct)

2. **Change call site in groupByNamespace()** (line 54):
   - Change `getNamespace(entry.returnType)` to `getNamespace(entry.name)`

Do NOT change getStrippedName() — it already correctly uses entry.name.

Root cause reference: entry.returnType is what a function returns (e.g. "SP.Web", "Edm.String", "Collection(SP.List)"), while entry.name is the fully-qualified function identifier (e.g. "Microsoft.SharePoint.Utilities.WebTemplateExtensions.SiteScriptUtility.GrantSiteDesignRights"). Namespace must be derived from name.
  </action>
  <verify>
1. `npx tsc --noEmit` — TypeScript compiles without errors (name is always present on ChildEntry, unlike optional returnType)
2. `npx vitest run` — all existing tests pass
3. Grep confirms the fix: `grep "getNamespace(entry.name)" app/src/components/navigation/Sidebar.tsx` returns a match
4. Grep confirms old code is gone: `grep "getNamespace(entry.returnType)" app/src/components/navigation/Sidebar.tsx` returns NO match
  </verify>
  <done>
getNamespace() accepts a required name parameter and the call site passes entry.name. Namespace groups are now derived from the item's fully-qualified name (matching Explore Types behavior), not from its return type. No malformed "Collection(SP" or "Edm" namespace headers appear.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` passes
2. Tests pass: `npx vitest run` passes
3. Code correctness: `getNamespace(entry.name)` appears in Sidebar.tsx, `getNamespace(entry.returnType)` does not
4. Function signature: getNamespace parameter is `name: string` (required), not `returnType?: string` (optional)
</verification>

<success_criteria>
- Namespace grouping derives from entry.name (fully-qualified function name)
- No references to entry.returnType remain in getNamespace call sites
- TypeScript compiles, all tests pass
- Gap from UAT is closed: namespace grouping matches Explore Types behavior
</success_criteria>

<output>
After completion, create `.planning/phases/09-explore-api-sidebar-polish/09-03-SUMMARY.md`
</output>
