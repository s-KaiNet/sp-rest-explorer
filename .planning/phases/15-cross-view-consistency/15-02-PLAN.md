---
phase: 15-cross-view-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/pages/TypesPage.tsx
  - app/src/components/types/TypesSidebarItem.tsx
autonomous: true
requirements: [XVEW-04, XVEW-05]

must_haves:
  truths:
    - "Explore Types welcome screen shows an orange/amber Braces Lucide icon — not a T in a tinted box"
    - "Explore Types sidebar entries show an orange/amber Braces Lucide icon left of the label — not a T text badge"
    - "Explore Types welcome hero follows the same bare TypeIcon(entity, lg) pattern as Explore API welcome hero"
    - "TypesSidebarItem layout mirrors SidebarItem: icon left of label, same spacing"
  artifacts:
    - path: "app/src/pages/TypesPage.tsx"
      provides: "TypeIcon-based welcome screen"
      contains: "TypeIcon"
    - path: "app/src/components/types/TypesSidebarItem.tsx"
      provides: "TypeIcon-based sidebar entries"
      contains: "TypeIcon"
  key_links:
    - from: "app/src/pages/TypesPage.tsx"
      to: "app/src/components/ui/type-icon.tsx"
      via: "import TypeIcon"
      pattern: "import.*TypeIcon.*from.*type-icon"
    - from: "app/src/components/types/TypesSidebarItem.tsx"
      to: "app/src/components/ui/type-icon.tsx"
      via: "import TypeIcon"
      pattern: "import.*TypeIcon.*from.*type-icon"
---

<objective>
Replace all text-based type indicators in Explore Types views with Lucide TypeIcon components, matching the patterns established in Explore API (Phase 14).

Purpose: Final views using the unified icon system — Explore Types welcome and sidebar match Explore API's visual language.
Output: TypesPage.tsx welcome hero and TypesSidebarItem.tsx using TypeIcon.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/components/ui/type-icon.tsx
@app/src/lib/api-types.ts
@app/src/pages/TypesPage.tsx
@app/src/components/types/TypesSidebarItem.tsx
@app/src/pages/ExplorePage.tsx (lines 136-138 — reference pattern for bare TypeIcon welcome hero)
@app/src/components/navigation/SidebarItem.tsx (reference pattern for icon-first sidebar item)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace T text with TypeIcon in Explore Types welcome and sidebar</name>
  <files>app/src/pages/TypesPage.tsx, app/src/components/types/TypesSidebarItem.tsx</files>
  <action>
**TypesPage.tsx — Welcome screen hero:**

Import TypeIcon:
```
import { TypeIcon } from '@/components/ui/type-icon'
```

Replace the welcome screen T-in-box:
```tsx
<div className="mb-5 flex h-[72px] w-[72px] items-center justify-center rounded-2xl bg-type-entity/10 text-[32px] font-extrabold text-type-entity">
  T
</div>
```
With bare TypeIcon following the exact same pattern as ExplorePage.tsx line 138:
```tsx
<TypeIcon type="entity" size="lg" className="mb-5" />
```
No background container — bare icon per user decision. This matches the Explore API welcome hero pattern exactly.

Also update the hint box at the bottom of the welcome screen. Currently it uses `text-green-800 dark:text-green-200` and `bg-type-entity/10` with a custom SVG cursor icon. Replace with:
- Change background to `bg-type-entity/5` (subtle entity-colored tint)
- Change text color to use entity-appropriate colors: `text-amber-800 dark:text-amber-200` (matching entity = orange/amber, NOT green which is for root)
- Keep the custom SVG cursor icon as-is (it's decorative, not a type indicator)

**TypesSidebarItem.tsx — Sidebar entries:**

Import TypeIcon:
```
import { TypeIcon } from '@/components/ui/type-icon'
```

Replace the T text badge:
```tsx
<span className="shrink-0 rounded bg-type-entity/10 px-1.5 py-0.5 text-xs font-semibold text-type-entity">
  T
</span>
```
With a bare TypeIcon matching the SidebarItem pattern:
```tsx
<TypeIcon type="entity" size="sm" />
```
No background container, no rounded box — just the icon. This mirrors `app/src/components/navigation/SidebarItem.tsx` line 25 exactly.

The gap-2 on the parent button already provides proper spacing between icon and label, matching SidebarItem's layout.
  </action>
  <verify>
Run `npx tsc --noEmit` in app/ — no type errors. Run `npx vite build` in app/ — build succeeds. Visually: Explore Types welcome shows orange Braces icon (not T), sidebar entries show small orange Braces icons left of labels (not T badge).
  </verify>
  <done>
Explore Types welcome screen displays bare TypeIcon(entity, lg) in orange/amber matching Explore API's pattern. Explore Types sidebar entries display TypeIcon(entity, sm) left of labels, consistent with Explore API sidebar. No T text badges or text symbols remain in Types views. Hint box text color corrected from green (root) to amber (entity).
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete unused text symbol code and verify no stray symbols remain</name>
  <files>app/src/components/search/CommandPalette.tsx, app/src/pages/HomePage.tsx, app/src/pages/TypesPage.tsx, app/src/components/types/TypesSidebarItem.tsx</files>
  <action>
After Plan 01 and Task 1 of this plan are complete, verify that no stray text symbols remain anywhere in the codebase's UI components.

Search across all .tsx files in app/src/ for any remaining text symbol patterns:
- `'<>'` or `{'<>'}` — old entity indicator
- `'ƒ'` or `{'\u0192'}` — old function indicator  
- `'>NAV<'` or `NAV</span>` — old nav property indicator
- `'>T<'` or `T</span>` where T is a type indicator — old entity type indicator
- `Root</span>` with pill badge classes — old Root pill

If any are found in UI-rendering code (not comments or string constants), they need to be replaced with TypeIcon.

Also check: Are there any components or utility functions that ONLY existed to produce old text symbols? If so, delete them. Based on codebase analysis, the text symbols were inline JSX — no separate components to delete.

Run a final grep to confirm zero remaining text symbols in UI rendering code. Document findings.
  </action>
  <verify>
Run grep across app/src/**/*.tsx for the symbol patterns listed above. Zero matches in JSX-rendering code (comments/type annotations don't count). Run `npx vite build` in app/ — build succeeds with no unused import warnings.
  </verify>
  <done>
No `<>`, `ƒ`, `NAV`, `T`, or Root pill text symbols remain in any UI component. All type indicators across all views use the unified TypeIcon system. Old text symbol code is fully removed.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` (in app/) — zero type errors
2. `npx vite build` (in app/) — build succeeds
3. Explore Types welcome shows orange Braces icon, not T text
4. Explore Types sidebar shows small orange Braces icons, not T badges
5. Zero text symbol occurrences (`<>`, `ƒ`, `NAV`, `T` badge, Root pill) in any UI component
</verification>

<success_criteria>
- Explore Types welcome screen displays TypeIcon(entity, lg) in orange/amber
- Explore Types sidebar entries display TypeIcon(entity, sm) consistent with icon system
- No stray text symbols exist anywhere in the application's UI components
- Hint box uses entity-appropriate amber colors (not root green)
</success_criteria>

<output>
After completion, create `.planning/phases/15-cross-view-consistency/15-02-SUMMARY.md`
</output>
