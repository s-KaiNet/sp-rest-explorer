---
phase: 13-icon-system-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/pages/HomePage.tsx
  - app/src/components/navigation/SidebarItem.tsx
  - app/src/pages/ExplorePage.tsx
  - app/src/components/search/CommandPalette.tsx
autonomous: true
gap_closure: true
requirements: [ICON-02, ICON-03]

must_haves:
  truths:
    - "Root-type UI elements (sidebar badge, home card, search pill, welcome icon) display in green using --type-root token"
    - "Entity-type UI elements remain orange/amber using --type-entity token"
    - "/_api welcome screen icon, root count, and hint box use root green — not function blue"
    - "Dark mode root elements display green, entity elements display orange/amber"
  artifacts:
    - path: "app/src/pages/HomePage.tsx"
      provides: "Root recently-visited card uses type-root, browse-all button uses type-root"
      contains: "text-type-root"
    - path: "app/src/components/navigation/SidebarItem.tsx"
      provides: "Root variant badge uses type-root"
      contains: "text-type-root"
    - path: "app/src/pages/ExplorePage.tsx"
      provides: "Welcome icon/count/hint use type-root instead of type-fn"
      contains: "text-type-root"
    - path: "app/src/components/search/CommandPalette.tsx"
      provides: "Root pill badge uses type-root"
      contains: "text-type-root"
  key_links:
    - from: "app/src/index.css"
      to: "all 4 component files"
      via: "Tailwind type-root utility classes"
      pattern: "(bg|text|border)-type-root"
---

<objective>
Fix root-type color references across all components after Phase 13's CSS token rename.

Purpose: Phase 13 commit b7aec41 changed --type-entity from green to orange and added --type-root (green), but no component was updated to reference the new type-root classes. Root items now incorrectly show orange instead of green. The /_api welcome screen still uses type-fn (blue) for root items instead of type-root (green).

Output: 7 class string replacements across 4 files — all root-type UI elements render green.
</objective>

<execution_context>
@./.Claude/get-shit-done/workflows/execute-plan.md
@./.Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-icon-system-foundation/13-01-SUMMARY.md
@.planning/phases/13-icon-system-foundation/13-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update root-type classes in HomePage and SidebarItem</name>
  <files>app/src/pages/HomePage.tsx, app/src/components/navigation/SidebarItem.tsx</files>
  <action>
Make these exact class string replacements:

**HomePage.tsx line 38** — Recently-visited card when `kind === 'root'`:
- Old: `colorClass = 'bg-type-entity/10 text-type-entity'`
- New: `colorClass = 'bg-type-root/10 text-type-root'`

**HomePage.tsx line 145** — "Browse all root endpoints" button hover classes:
- Old: `hover:border-type-fn hover:bg-type-fn/5 hover:text-type-fn`
- New: `hover:border-type-root hover:bg-type-root/5 hover:text-type-root`

**SidebarItem.tsx line 27** — Root variant sidebar badge:
- Old: `bg-type-entity/10 px-1.5 py-0.5 text-xs font-medium font-mono text-type-entity`
- New: `bg-type-root/10 px-1.5 py-0.5 text-xs font-medium font-mono text-type-root`

Do NOT change the entity case on HomePage.tsx line 44 (`kind === 'entity'`) — that correctly uses type-entity (orange).
  </action>
  <verify>
Run: `node -e "const fs=require('fs'); const h=fs.readFileSync('app/src/pages/HomePage.tsx','utf8'); const s=fs.readFileSync('app/src/components/navigation/SidebarItem.tsx','utf8'); const hRoot=(h.match(/text-type-root/g)||[]).length; const hEntity=(h.match(/text-type-entity/g)||[]).length; const sRoot=(s.match(/text-type-root/g)||[]).length; console.log('HomePage: root='+hRoot+' entity='+hEntity+' SidebarItem: root='+sRoot); console.log(hRoot>=2 && hEntity>=1 && sRoot>=1 ? 'PASS' : 'FAIL')"`

Expected: HomePage: root=2 entity=1 SidebarItem: root=1 → PASS
  </verify>
  <done>Root recently-visited cards show green, browse-all button hovers green, sidebar root badge shows green. Entity cards remain orange.</done>
</task>

<task type="auto">
  <name>Task 2: Update root-type classes in ExplorePage and CommandPalette</name>
  <files>app/src/pages/ExplorePage.tsx, app/src/components/search/CommandPalette.tsx</files>
  <action>
Make these exact class string replacements:

**ExplorePage.tsx line 138** — /_api welcome icon box:
- Old: `bg-type-fn/10 text-[28px] font-extrabold text-type-fn`
- New: `bg-type-root/10 text-[28px] font-extrabold text-type-root`

**ExplorePage.tsx line 155** — Root Endpoints count:
- Old: `text-[28px] font-bold text-type-fn`  (the one inside the Root Endpoints stat, NOT the Functions stat on line 163)
- New: `text-[28px] font-bold text-type-root`

**ExplorePage.tsx line 173** — Hint box background:
- Old: `bg-type-fn/10 px-[18px] py-3 text-[13px] text-blue-800 dark:text-blue-200`
- New: `bg-type-root/10 px-[18px] py-3 text-[13px] text-green-800 dark:text-green-200`

Do NOT change the Functions count stat on line 163 — that correctly uses text-type-fn (blue).

**CommandPalette.tsx line 399** — Root pill badge in search results:
- Old: `bg-type-entity/10 px-1.5 py-0.5 text-[10px] font-semibold text-type-entity`
- New: `bg-type-root/10 px-1.5 py-0.5 text-[10px] font-semibold text-type-root`
  </action>
  <verify>
Run: `node -e "const fs=require('fs'); const e=fs.readFileSync('app/src/pages/ExplorePage.tsx','utf8'); const c=fs.readFileSync('app/src/components/search/CommandPalette.tsx','utf8'); const eRoot=(e.match(/type-root/g)||[]).length; const eFn=(e.match(/type-fn/g)||[]).length; const cRoot=(c.match(/text-type-root/g)||[]).length; const cEntity=(c.match(/text-type-entity/g)||[]).length; console.log('ExplorePage: root='+eRoot+' fn='+eFn+' CommandPalette: root='+cRoot+' entity='+cEntity); console.log(eRoot>=4 && eFn>=2 && cRoot>=1 && cEntity>=1 ? 'PASS' : 'FAIL')"`

Expected: ExplorePage has >=4 type-root refs (icon bg+text, count, hint bg) and >=2 type-fn refs (functions count + stat). CommandPalette has >=1 type-root and >=1 type-entity remaining (line 349 entity search icon is correct) → PASS
  </verify>
  <done>/_api welcome screen shows green icon, green root count, green hint box. Functions count stays blue. Search root pill shows green instead of orange.</done>
</task>

</tasks>

<verification>
After both tasks, run TypeScript check to ensure no broken class references:
```
npx tsc --noEmit
```

Visual verification: In the running app:
1. Navigate to home page — recently-visited root items show GREEN badges, entity items show ORANGE
2. "Browse all root endpoints" button hovers to GREEN
3. Navigate to /_api — welcome icon is GREEN, root endpoints count is GREEN, functions count is BLUE, hint box background is green-tinted
4. Open Cmd+K search, find a root result — "Root" pill badge is GREEN
5. Toggle dark mode — all root elements remain green, all entity elements remain orange
</verification>

<success_criteria>
- All root-type UI elements render in green (--type-root, OKLCH hue ~155)
- All entity-type UI elements render in orange/amber (--type-entity, OKLCH hue ~80)
- /_api welcome screen uses root green for icon/count, function blue only for function count
- Dark mode shows same correct color assignments
- TypeScript compiles clean (npx tsc --noEmit passes)
</success_criteria>

<output>
After completion, create `.planning/phases/13-icon-system-foundation/13-02-SUMMARY.md`
</output>
