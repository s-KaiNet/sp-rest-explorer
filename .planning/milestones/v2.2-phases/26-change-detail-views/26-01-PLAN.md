---
phase: 26-change-detail-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/changelog/ChangeBadge.tsx
  - app/src/components/changelog/RootFunctionsTable.tsx
  - app/src/components/changelog/EntityChangeCard.tsx
autonomous: true
requirements: [VIEW-03, VIEW-04, VIEW-06]

must_haves:
  truths:
    - "ChangeBadge renders color-coded pill labels for added/updated/removed change types"
    - "RootFunctionsTable renders a sorted table with function name, return type, and change badge columns"
    - "EntityChangeCard renders expandable card with property, navigation property, and function sub-sections"
    - "All components accept typed diff data from Phase 24 types"
  artifacts:
    - path: "app/src/components/changelog/ChangeBadge.tsx"
      provides: "Reusable change-type badge component"
      min_lines: 15
    - path: "app/src/components/changelog/RootFunctionsTable.tsx"
      provides: "Root functions table with 3 columns"
      min_lines: 30
    - path: "app/src/components/changelog/EntityChangeCard.tsx"
      provides: "Expandable entity card with sub-sections"
      min_lines: 60
  key_links:
    - from: "app/src/components/changelog/ChangeBadge.tsx"
      to: "@/lib/diff"
      via: "ChangeType import"
      pattern: "import.*ChangeType.*from.*@/lib/diff"
    - from: "app/src/components/changelog/RootFunctionsTable.tsx"
      to: "app/src/components/changelog/ChangeBadge.tsx"
      via: "renders ChangeBadge per row"
      pattern: "<ChangeBadge"
    - from: "app/src/components/changelog/EntityChangeCard.tsx"
      to: "app/src/components/changelog/ChangeBadge.tsx"
      via: "renders ChangeBadge in header and rows"
      pattern: "<ChangeBadge"
---

<objective>
Create the three changelog detail components — ChangeBadge, RootFunctionsTable, and EntityChangeCard — that will render the actual change content on the API Changelog page.

Purpose: These components consume Phase 24 diff types (DiffEntity, DiffFunction, DiffPropertyChange, ChangeType) and render them as visual change details. The page shell (Phase 25) already exists with a placeholder for this content.

Output: Three new component files in `app/src/components/changelog/`
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-change-detail-views/26-CONTEXT.md

# Source files to reference
@app/src/lib/diff/types.ts
@app/src/components/entity/CollapsibleSection.tsx
@app/src/components/ui/code-text.tsx
@app/src/components/entity/PropertiesTable.tsx
@app/src/components/entity/MethodsTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChangeBadge component and RootFunctionsTable</name>
  <files>app/src/components/changelog/ChangeBadge.tsx, app/src/components/changelog/RootFunctionsTable.tsx</files>
  <action>
Create `app/src/components/changelog/` directory and two components:

**ChangeBadge.tsx:**
- Props: `{ changeType: ChangeType }` — import ChangeType from `@/lib/diff`
- Renders a pill-shaped badge (rounded-full) with subtle fill background and colored text
- Color mapping per user decision:
  - `added` → green: `bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-400`
  - `updated` → blue: `bg-blue-100 text-blue-700 dark:bg-blue-900/30 dark:text-blue-400`
  - `removed` → red: `bg-red-100 text-red-700 dark:bg-red-900/30 dark:text-red-400`
- Labels: "Added", "Updated", "Removed" (capitalized, no "New" label per user decision)
- Styling: `inline-flex items-center rounded-full px-2 py-0.5 text-[11px] font-medium`

**RootFunctionsTable.tsx:**
- Props: `{ functions: DiffFunction[] }` — import DiffFunction from `@/lib/diff`
- Three columns per user decision: Function Name, Return Type, Change Badge
- One mixed table sorted alphabetically by name (not grouped by change type) per user decision
- Function names rendered with `CodeText` component (variant="fn") from `@/components/ui/code-text`
- Return types rendered with `CodeText` (default variant) — plain monospace, no linking (linking is Phase 27)
- Change badge column renders `ChangeBadge` component
- Table styling matches existing pattern from PropertiesTable.tsx: `w-full border-collapse text-sm`, header with `text-[11px] font-semibold uppercase tracking-wider text-muted-foreground`, rows with `hover:bg-muted/50` and `border-b border-border/50`
- Column widths: Function Name 45%, Return Type 35%, Change 20%
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check both files exist and contain expected imports</manual>
  </verify>
  <done>ChangeBadge renders 3 color-coded pill badges. RootFunctionsTable renders a sorted 3-column table with CodeText names, CodeText return types, and ChangeBadge per row.</done>
</task>

<task type="auto">
  <name>Task 2: Create EntityChangeCard component</name>
  <files>app/src/components/changelog/EntityChangeCard.tsx</files>
  <action>
Create EntityChangeCard component:

- Props: `{ entity: DiffEntity }` — import DiffEntity, DiffPropertyChange from `@/lib/diff`
- **Card header:** Entity name in bold monospace text with a `ChangeBadge` showing the entity's changeType. Use a clickable header that toggles expand/collapse.
- **Card starts expanded by default** per user decision — use `useState(true)` for the expanded state.
- Card wrapper: `rounded-lg border border-border bg-background` with subtle shadow or none (Claude's discretion per CONTEXT.md).

**Inside the card (when expanded), three sub-sections using mini inline headers** (NOT CollapsibleSection — that's too heavy for nested use; use simple labeled groups):

1. **Properties** sub-section:
   - Header: "Properties" with count in parentheses, e.g., "Properties (5)"
   - Render each DiffPropertyChange as a row: `name` in font-mono font-medium, `typeName` in font-mono text-muted-foreground, and `ChangeBadge`
   - Sorted alphabetically by name per user decision
   - Hide sub-section entirely if 0 items (Claude's discretion from CONTEXT.md — hide when empty)

2. **Navigation Properties** sub-section:
   - Same layout as Properties, using entity.navigationProperties
   - Header: "Navigation Properties (N)"
   - Hide if 0 items

3. **Functions** sub-section:
   - Same layout as Properties, using entity.functions
   - Header: "Functions (N)"
   - Hide if 0 items

**For added entities** (changeType === 'added'): list ALL properties — no condensed summary per user decision. The same rendering applies regardless of changeType.

**Property row layout:** Use a simple flex row or table-like grid:
- Name: `font-mono font-medium text-sm` (left)
- TypeName: `font-mono text-xs text-muted-foreground` (middle)
- ChangeBadge: right-aligned

Sub-section headers: `text-xs font-semibold text-muted-foreground uppercase tracking-wider` with a thin bottom border, matching the existing section header convention.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify EntityChangeCard has expand/collapse, 3 sub-sections, and renders ChangeBadge in header and rows</manual>
  </verify>
  <done>EntityChangeCard renders an expandable card with entity name + badge header, and three alphabetically-sorted sub-sections (Properties, Navigation Properties, Functions) that hide when empty. All property rows show name + typeName + ChangeBadge.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 3 files exist in `app/src/components/changelog/`
- ChangeBadge accepts ChangeType and renders green/blue/red pills
- RootFunctionsTable accepts DiffFunction[] and renders sorted 3-column table
- EntityChangeCard accepts DiffEntity and renders expandable card with sub-sections
</verification>

<success_criteria>
Three new components created in `app/src/components/changelog/` that accept Phase 24 diff types and render color-coded change details. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/26-change-detail-views/26-01-SUMMARY.md`
</output>
