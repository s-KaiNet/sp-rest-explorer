---
phase: 10-home-screens-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/pages/HomePage.tsx
  - app/src/hooks/use-recently-visited.ts
  - app/src/hooks/index.ts
  - app/src/pages/TypesPage.tsx
autonomous: true

must_haves:
  truths:
    - "Home page shows favicon.svg icon inline to the left of the title at 32-40px size"
    - "Home page stats show hardcoded approximations: 3.5k+ functions, 2.4k entities, 11k+ properties, 60k+ endpoints"
    - "Home page stats render instantly with no loading delay or ellipsis"
    - "Recently visited section shows both API endpoint entries and Types entries in one chronological list"
    - "Types entries display a purple T icon, visually distinct from API endpoint icons"
    - "Max 12 items shared between API and Types visits"
  artifacts:
    - path: "app/src/pages/HomePage.tsx"
      provides: "Home page with inline icon, hardcoded stats, and mixed recently visited"
    - path: "app/src/hooks/use-recently-visited.ts"
      provides: "RecentlyVisitedItem with 'entity' kind support"
    - path: "app/src/pages/TypesPage.tsx"
      provides: "Tracks type visits via addVisit on type selection"
  key_links:
    - from: "app/src/pages/TypesPage.tsx"
      to: "app/src/hooks/use-recently-visited.ts"
      via: "useRecentlyVisited().addVisit() call on type selection"
      pattern: "addVisit.*kind.*entity"
    - from: "app/src/pages/HomePage.tsx"
      to: "app/src/hooks/use-recently-visited.ts"
      via: "useRecentlyVisited() for mixed items list"
      pattern: "item\\.kind === 'entity'"
---

<objective>
Update the home page with proper branding (inline favicon icon, hardcoded approximate stats) and expand recently visited to include Types entries alongside API endpoints.

Purpose: Fulfills HOME-01 (icon branding), HOME-02 (approximate stats), and SIDE-04 (recently visited expansion with Types entries). Makes the home page feel polished and complete.
Output: Updated HomePage.tsx with icon + static stats, expanded RecentlyVisitedItem type, Types visit tracking in TypesPage.tsx.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/pages/HomePage.tsx
@app/src/hooks/use-recently-visited.ts
@app/src/pages/TypesPage.tsx
@app/src/hooks/index.ts
@app/public/favicon.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home page icon branding and hardcoded stats</name>
  <files>app/src/pages/HomePage.tsx</files>
  <action>
  Modify HomePage.tsx to:

  1. **Add inline favicon icon** next to the title (per user decision: "Add favicon.svg icon inline to the left of the title, larger/prominent 32-40px range"):
     - Import the favicon SVG: `import faviconUrl from '/favicon.svg'` (Vite public dir asset)
     - In the hero section, change the `<h1>` to a flex row containing:
       - `<img src={faviconUrl} alt="" className="h-9 w-9" />` (36px, within 32-40px range)
       - The existing title text "SharePoint REST API Explorer"
     - Use `flex items-center justify-center gap-3` on the h1 wrapper

  2. **Replace computed stats with hardcoded approximations** (per user decision: "Replace all computed stats with hardcoded approximate strings"):
     - Remove the `metadata` import (`useMetadataSnapshot`) and all stat computation (`functionCount`, `entityCount`, `navPropertyCount`, `rootCount`)
     - Replace the stats row with 4 static items in a single horizontal row with even spacing:
       - "3.5k+" functions (blue dot, `bg-type-fn`)
       - "2.4k" entities (green dot, `bg-type-entity`)
       - "11k+" properties (purple dot, `bg-type-nav`)
       - "60k+" endpoints (new — use a neutral dot or reuse one of the existing colors; use `bg-muted-foreground` for a neutral approach)
     - The subtitle text ("Browse, search, and explore...") stays unchanged per user decision
     - Update the "Browse all root endpoints" button to remove the dynamic `rootCount` badge since we no longer have metadata. Keep the button text and styling, just remove the count span.

  3. **Keep everything else intact**: search trigger, recently visited section (modified in Task 2), layout structure.
  </action>
  <verify>Run `cd app && npx tsc --noEmit` — no type errors. Visually: home page shows icon left of title, 4 hardcoded stat values, no loading ellipsis.</verify>
  <done>Home page renders favicon.svg inline left of title at ~36px, shows "3.5k+ functions", "2.4k entities", "11k+ properties", "60k+ endpoints" as static text with no metadata dependency, and the browse-all button has no dynamic count.</done>
</task>

<task type="auto">
  <name>Task 2: Expand recently visited to include Types entries</name>
  <files>app/src/hooks/use-recently-visited.ts, app/src/hooks/index.ts, app/src/pages/TypesPage.tsx, app/src/pages/HomePage.tsx</files>
  <action>
  **Step 1 — Update RecentlyVisitedItem type** in `use-recently-visited.ts`:
  - Add `'entity'` to the `kind` union: `kind: 'function' | 'navProperty' | 'root' | 'entity'`
  - No other changes to the hook logic — the existing `addVisit`, `loadFromStorage`, `saveToStorage`, and `MAX_ITEMS = 12` all work as-is since the item shape is the same.

  **Step 2 — Track type visits in TypesPage.tsx**:
  - Import `useRecentlyVisited` from `@/hooks`
  - In the `TypesPage` component, call `const { addVisit } = useRecentlyVisited()`
  - In `handleTypeSelect`, after `navigate(...)`, call:
    ```ts
    addVisit({
      name: type.fullName.split('.').pop() || type.fullName,
      path: `/entity/${encodeURIComponent(type.fullName)}`,
      kind: 'entity',
    })
    ```
  - This records the type visit with the short name (last segment after dot) as display name, the full entity path, and kind `'entity'`.

  **Step 3 — Update RecentlyVisitedCard in HomePage.tsx** to handle `'entity'` kind:
  - In the `RecentlyVisitedCard` component, add an `else if (item.kind === 'entity')` branch:
    ```ts
    } else if (item.kind === 'entity') {
      icon = 'T'
      colorClass = 'bg-type-entity/10 text-type-entity'
    }
    ```
  - Place this before the final `else` (navProperty). The purple "T" icon per user decision: "Types entries use a purple T icon matching the Explore Types branding". Wait — the user said "purple T" but `type-entity` is green. Looking at CONTEXT.md: "Types entries use a purple T icon matching the Explore Types branding, visually distinct from API endpoint icons." But the Explore Types page actually uses `bg-type-entity/10 text-type-entity` which is green. The user's intent is to match the Explore Types branding — so use the same green entity color (`bg-type-entity/10 text-type-entity`) that TypesPage.tsx uses. The "purple" in the context likely refers to the overall types section association, but the actual implementation should match the existing TypesPage welcome screen pattern (green "T" in green box).

  **Step 4 — Update navigate handler for entity items in HomePage.tsx**:
  - The existing `onClick={() => navigate(item.path)}` already works since entity items store `/entity/{fullName}` as their path. No change needed.

  Per user decision: "No dedicated Explore Types link needed in the recently visited section — the types entries themselves serve as navigation."
  </action>
  <verify>Run `cd app && npx tsc --noEmit` — no type errors. Test: navigate to Explore Types, click a type, go back to home — the type appears in recently visited with a "T" icon. API endpoint visits still appear correctly.</verify>
  <done>RecentlyVisitedItem.kind includes 'entity', TypesPage tracks visits on type selection, HomePage renders entity items with "T" icon in entity-green color, mixed chronological list with max 12 shared items.</done>
</task>

</tasks>

<verification>
1. `cd app && npx tsc --noEmit` passes with no errors
2. `cd app && npm run build` succeeds
3. Home page shows favicon icon inline left of title
4. Home page shows 4 hardcoded stat values with no loading state
5. Browse all root endpoints button works (no count badge)
6. After visiting a type in Explore Types, returning to home shows it in recently visited
7. Recently visited shows mixed API + Types entries sorted by recency
8. Entity items in recently visited show "T" icon with green styling
</verification>

<success_criteria>
- HOME-01: Favicon.svg displayed inline left of title at 32-40px
- HOME-02: Stats show "3.5k+ functions", "2.4k entities", "11k+ properties", "60k+ endpoints"
- SIDE-04: Recently visited includes Types entries with entity kind and T icon
- No TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-home-screens-visual-polish/10-01-SUMMARY.md`
</output>
