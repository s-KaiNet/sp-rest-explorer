---
phase: 09-explore-api-sidebar-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/entity/EntityDetail.tsx
  - app/src/components/types/ComplexTypeDetail.tsx
  - app/src/components/entity/MethodsTable.tsx
  - app/src/pages/ExplorePage.tsx
autonomous: true

must_haves:
  truths:
    - "User no longer sees 'Entity Type' badge in entity detail views"
    - "User no longer sees 'Complex Type' badge in complex type detail views"
    - "User no longer sees 'Composable' badge on functions in methods tables or inline function views"
    - "Detail view heading area tightens up naturally after badge removal"
  artifacts:
    - path: "app/src/components/entity/EntityDetail.tsx"
      provides: "Entity detail view without 'Entity Type' badge"
      contains: "text-[22px] font-bold"
    - path: "app/src/components/types/ComplexTypeDetail.tsx"
      provides: "Complex type detail view without 'Complex Type' badge"
      contains: "text-[22px] font-bold"
    - path: "app/src/components/entity/MethodsTable.tsx"
      provides: "Methods table without 'COMPOSABLE' badge"
    - path: "app/src/pages/ExplorePage.tsx"
      provides: "Inline function view without 'COMPOSABLE' badge"
  key_links:
    - from: "app/src/components/entity/EntityDetail.tsx"
      to: "heading layout"
      via: "flex items-center gap container"
      pattern: "text-\\[22px\\] font-bold"
---

<objective>
Remove "Entity Type", "Complex Type", and "Composable" badges from all locations in the app.

Purpose: Per user decision, these badge types add visual clutter without providing useful information. Item names and context are sufficient for differentiation. Removing them declutters the UI and tightens heading areas.

Output: Four files updated — EntityDetail.tsx, ComplexTypeDetail.tsx, MethodsTable.tsx, ExplorePage.tsx — with all three badge types fully removed.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/components/entity/EntityDetail.tsx
@app/src/components/types/ComplexTypeDetail.tsx
@app/src/components/entity/MethodsTable.tsx
@app/src/pages/ExplorePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Entity Type and Complex Type badges from detail views</name>
  <files>app/src/components/entity/EntityDetail.tsx, app/src/components/types/ComplexTypeDetail.tsx</files>
  <action>
  In EntityDetail.tsx:
  - Remove the "Entity Type" badge `<span>` element from the header area (lines 70-72). The badge is inside a flex container with the h2 heading.
  - After removal, the header becomes just the h2 element. Remove the wrapping `<div className="mb-1 flex items-center gap-3.5">` and keep just the `<h2>` with `className="mb-1 text-[22px] font-bold"` (move the mb-1 from the wrapper div to the h2).

  In ComplexTypeDetail.tsx:
  - Remove the "Complex Type" badge `<span>` element from the header area (lines 50-52). Same pattern.
  - Same simplification: remove the flex wrapper div, keep just the h2 with `className="mb-1 text-[22px] font-bold"`.

  Per user decision: Nothing replaces the removed badges — item names are sufficient. The heading area tightens up naturally.
  </action>
  <verify>
  Run `npm run build` in the app directory — no TypeScript or build errors.
  Grep for "Entity Type" and "Complex Type" in all .tsx files — zero matches (excluding comments and non-badge text like "Entity Types" stats label on HowItWorksPage).
  </verify>
  <done>
  EntityDetail.tsx header shows only the entity name (no "Entity Type" badge). ComplexTypeDetail.tsx header shows only the type name (no "Complex Type" badge). Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Composable badges from methods table and inline function view</name>
  <files>app/src/components/entity/MethodsTable.tsx, app/src/pages/ExplorePage.tsx</files>
  <action>
  In MethodsTable.tsx:
  - Remove the COMPOSABLE badge span (lines 67-70) inside the returns `<td>`. This is the `{fn.isComposable && (...)}` block.
  - Update the JSDoc comment on line 11 to remove "Composable methods show COMPOSABLE badge." mention.
  - The returns column still shows the return type via TypeLink — only the badge is removed.

  In ExplorePage.tsx:
  - Remove the COMPOSABLE badge span (lines 172-175) inside the inline function return type display. This is the `{currentFunction.isComposable && (...)}` block.
  - The return type TypeLink remains — only the badge is removed.

  Per user decision: "Composable" badge is removed entirely. Nothing replaces it.
  </action>
  <verify>
  Run `npm run build` in the app directory — no errors.
  Grep for "COMPOSABLE" (case-sensitive) and "isComposable" rendered in JSX across .tsx files — the `isComposable` property may still be used in logic (api-tree-walk.ts) but should NOT appear in any JSX render output.
  </verify>
  <done>
  MethodsTable.tsx no longer renders COMPOSABLE badge next to return types. ExplorePage.tsx inline function view no longer renders COMPOSABLE badge. Build passes. No visual badge for composable functions anywhere in the app.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. Grep confirms no "Entity Type", "Complex Type" badge text in rendered JSX (non-comment, non-stats-label)
3. Grep confirms no "COMPOSABLE" badge text in rendered JSX
4. The `isComposable` property is still used in api-tree-walk.ts for BFS traversal logic — this is correct and unchanged
</verification>

<success_criteria>
- Zero instances of "Entity Type", "Complex Type", or "COMPOSABLE" badges rendered in the app UI
- Detail view headers show only entity/type names
- Methods table shows only return types without composable indicators
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-explore-api-sidebar-polish/09-01-SUMMARY.md`
</output>
