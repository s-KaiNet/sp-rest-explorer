---
phase: 22-switch-to-compressed-data-source
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/src/lib/constants.ts
  - app/src/lib/metadata/boot.ts
autonomous: false
requirements:
  - DSRC-01
  - DSRC-02
  - DCMP-01
  - DCMP-02
  - DCMP-03
  - DCMP-04
  - DCMP-05

must_haves:
  truths:
    - "App boots from cold start: fetches compressed blob, decompresses, parses, renders home screen"
    - "App boots from warm start: loads cached data instantly, background-revalidates with compressed blob"
    - "Network transfer for metadata is ~557KB (compressed) instead of ~2.2MB"
    - "All existing functionality works identically after the switch"
    - "METADATA_URL is defined in a single location (constants.ts) pointing to new compressed blob"
  artifacts:
    - path: "app/package.json"
      provides: "lz-string production dependency"
      contains: "lz-string"
    - path: "app/src/lib/constants.ts"
      provides: "METADATA_URL pointing to compressed blob"
      contains: "sprestapiexplorernew.blob.core.windows.net/api-files/metadata.latest.zip.json"
    - path: "app/src/lib/metadata/boot.ts"
      provides: "Decompression step in fetchFresh pipeline"
      contains: "decompressFromUTF16"
  key_links:
    - from: "app/src/lib/metadata/boot.ts"
      to: "lz-string"
      via: "named import"
      pattern: "import.*decompressFromUTF16.*from.*lz-string"
    - from: "app/src/lib/metadata/boot.ts"
      to: "app/src/lib/constants.ts"
      via: "METADATA_URL import (unchanged)"
      pattern: "import.*METADATA_URL.*from.*constants"
    - from: "fetchFresh()"
      to: "decompressFromUTF16"
      via: "pipeline: res.text() → decompress → JSON.parse"
      pattern: "decompressFromUTF16.*res\\.text\\(\\)|text.*decompressFromUTF16"
---

<objective>
Switch the frontend metadata source from the old uncompressed blob to the new backend's compressed blob, adding lz-string decompression to the fetch pipeline.

Purpose: Deliver ~75% network savings (~557KB vs ~2.2MB) by using the compressed metadata blob produced by the v2.0 backend, while preserving all existing app behavior and the stale-while-revalidate caching strategy.

Output: Updated constants.ts, boot.ts with decompression, lz-string dependency installed — app boots identically but with significantly smaller network payload.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-switch-to-compressed-data-source/22-CONTEXT.md

@app/src/lib/constants.ts
@app/src/lib/metadata/boot.ts
@app/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lz-string and wire decompression into fetch pipeline</name>
  <files>app/package.json, app/src/lib/constants.ts, app/src/lib/metadata/boot.ts</files>
  <action>
Three changes — all required together for the app to boot:

**1. Install lz-string dependency (app/ directory):**
```bash
cd app && npm install lz-string && npm install -D @types/lz-string
```

**2. Update METADATA_URL in `app/src/lib/constants.ts`:**
Change the URL from:
```
https://sprestapiexplorer.blob.core.windows.net/api-files/metadata.latest.json
```
to:
```
https://sprestapiexplorernew.blob.core.windows.net/api-files/metadata.latest.zip.json
```
Leave CACHE_KEY and API_PREFIX unchanged.

**3. Add decompression to `fetchFresh()` in `app/src/lib/metadata/boot.ts`:**

Add import at the top (use named import, matching backend convention):
```typescript
import { decompressFromUTF16 } from 'lz-string'
```

Replace the `fetchFresh()` function body with the new pipeline per user decision:
```typescript
async function fetchFresh(): Promise<Metadata> {
  const res = await fetch(METADATA_URL)
  if (!res.ok) {
    throw new Error(`HTTP ${res.status}: ${res.statusText}`)
  }
  const compressed = await res.text()
  const json = decompressFromUTF16(compressed)
  if (json === null) {
    throw new Error('Decompression failed: decompressFromUTF16 returned null')
  }
  return JSON.parse(json) as Metadata
}
```

Key decisions (from CONTEXT.md — do NOT deviate):
- Use `res.text()` NOT `res.json()` — compressed blob is not valid JSON
- If `decompressFromUTF16` returns null, throw (treat as error)
- No console.log for decompression stats
- No automatic retry — user clicks retry manually via existing error state
- No changes to `bootMetadata()`, `retryBoot()`, `hydrate()`, or cache logic
- Same CACHE_KEY — natural overwrite via stale-while-revalidate
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit && npx vite build</automated>
    <manual>TypeScript compiles without errors, Vite build succeeds</manual>
  </verify>
  <done>
- lz-string is in app/package.json dependencies
- @types/lz-string is in app/package.json devDependencies
- METADATA_URL in constants.ts points to `sprestapiexplorernew.blob.core.windows.net/api-files/metadata.latest.zip.json`
- fetchFresh() pipeline is: fetch → res.text() → decompressFromUTF16 → JSON.parse
- decompressFromUTF16 null check throws descriptive error
- Build passes with zero errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify compressed metadata boot in browser</name>
  <what-built>Frontend now fetches compressed metadata blob (~557KB) from the new storage account, decompresses with lz-string, and hydrates the app. Both cold and warm boot paths work through the new decompression pipeline.</what-built>
  <how-to-verify>
1. Clear browser data for the app (DevTools → Application → Storage → Clear site data) to test cold start
2. Run `cd app && npm run dev` and open the app in browser
3. Open DevTools Network tab, filter by "metadata"
4. Reload the page — verify:
   - A request goes to `sprestapiexplorernew.blob.core.windows.net/api-files/metadata.latest.zip.json`
   - Transfer size is ~557KB (not ~2.2MB)
   - App loads successfully — home screen shows live stats, entity counts
5. Navigate around: click an API endpoint, browse entities, open search, explore types — everything should work identically
6. Reload the page again (warm start) — verify:
   - App loads instantly from cache
   - Background revalidation request appears in Network tab to the same compressed URL
7. Check console for any errors — there should be none
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds in app/ directory (TypeScript + Vite)
- METADATA_URL grep returns exactly 1 result in constants.ts pointing to new URL
- `decompressFromUTF16` import exists in boot.ts
- No references to old URL `sprestapiexplorer.blob.core.windows.net` remain in src/
- fetchFresh() uses `res.text()` not `res.json()`
</verification>

<success_criteria>
- App boots from cold start using compressed blob from new storage account
- App boots from warm start using IndexedDB cache, revalidates in background with compressed blob
- Network transfer is ~557KB instead of ~2.2MB (verifiable in DevTools)
- All existing functionality works: entity browsing, search, type exploration, navigation, detail panels
- METADATA_URL defined once in constants.ts pointing to compressed blob
- Build passes with zero TypeScript or Vite errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-switch-to-compressed-data-source/22-01-SUMMARY.md`
</output>
