---
phase: 07.1-fix-search-experience
verified: 2026-02-14T18:30:00Z
status: passed
score: 14/14 must-haves verified (automated)
re_verification: false

human_verification:
  - test: "Open search (Ctrl+K), type 'Web' — verify Entities group shows SP.Web, click navigates to /entity/SP.Web"
    expected: "Entity detail page loads at /entity/SP.Web"
    why_human: "Requires live metadata fetch, visual rendering, route navigation"
  - test: "Type 'GetById' — verify API Endpoints group shows results with _api/ paths, click navigates to /_api/<path>"
    expected: "API browse page loads at correct _api/ path"
    why_human: "Requires live metadata fetch, visual rendering, route navigation"
  - test: "Search 'Get' — verify Show more button appears, click expands, groups are collapsible, collapse resets on new query"
    expected: "5 initial results per group, Show more expands, collapse toggle works, resets on query change"
    why_human: "Interactive UI state behavior"
  - test: "Arrow keys navigate across both groups seamlessly (group headers skipped), Enter selects"
    expected: "Seamless keyboard navigation, Enter triggers navigate"
    why_human: "Keyboard interaction behavior"
  - test: "Verify Root badge on root functions and ƒ vs NAV icon distinction"
    expected: "Root endpoints show Root badge, functions show ƒ icon, nav properties show NAV icon"
    why_human: "Visual rendering verification"
---

# Phase 07.1: Fix Search Experience — Verification Report

**Phase Goal:** Fix search result navigation UX so clicking any search result reliably navigates to the correct destination. Restructure search indexing to two groups (Entities, API Endpoints) with pre-computed navigation paths.
**Verified:** 2026-02-14T18:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

#### Plan 01 — BFS Tree-Walk and Search Index Backend

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | BFS tree-walk produces API endpoint entries from metadata by traversing composable root functions through entity nav properties and bound functions | ✓ VERIFIED | `api-tree-walk.ts` (129 lines): seeds from `fn.isRoot`, traverses `entity.navigationProperties` and `entity.functionIds`, follows composable return types |
| 2 | Search index contains two kinds of documents: 'entity' and 'endpoint' | ✓ VERIFIED | `search-index.ts` lines 29-53: entity docs from `metadata.entities`, endpoint docs from `buildApiEndpointIndex()` |
| 3 | Entity documents are searchable by both short name and full OData name | ✓ VERIFIED | `search-index.ts` line 16: `fields: ['name', 'fullName']`; entity docs set both `name` and `fullName` (lines 31-32) |
| 4 | Endpoint documents are searchable by leaf name only — path stored but NOT indexed for search | ✓ VERIFIED | `search-index.ts` line 46: `fullName: ''` for endpoints; path is in `storeFields` (line 17) but not in `fields` |
| 5 | Endpoints use per-path ancestor tracking with depth limit (evolved from identity-based dedup) | ✓ VERIFIED | `api-tree-walk.ts` lines 38-43: per-path `ancestors: Set<string>` in BFS queue; `MAX_DEPTH = 4` (line 8); cycle detection on lines 93, 119 |
| 6 | Non-composable root functions are indexed as leaf endpoints (no child traversal) | ✓ VERIFIED | `api-tree-walk.ts` lines 46-67: all root functions get entries; only composable ones with known return types enqueue for BFS (line 63) |
| 7 | Boot sequence correctly wires LookupMaps → search index builder | ✓ VERIFIED | `boot.ts` line 71: `initSearchIndex(data, getLookupMaps()!)` — passes LookupMaps from singleton |

#### Plan 02 — CommandPalette 2-Group UI

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 8 | Search results display in two groups: 'Entities' first, 'API Endpoints' second | ✓ VERIFIED | `CommandPalette.tsx` lines 337-356: `<SearchGroup heading="Entities">` first, `<SearchGroup heading="API Endpoints">` second |
| 9 | Each group is collapsible and collapse state resets on new query | ✓ VERIFIED | Lines 155-166: `entitiesCollapsed`/`endpointsCollapsed` state + `useEffect` resets all four states on `debouncedQuery` change |
| 10 | Each group shows initial batch of 5 results with 'Show more' to expand | ✓ VERIFIED | `INITIAL_SHOW = 5` (line 29); `SearchGroup` shows `results.slice(0, INITIAL_SHOW)` (line 107) with "Show N more…" `CommandItem` (lines 131-137) |
| 11 | Entity results show short name + full OData namespace, navigate to /entity/<fullName> | ✓ VERIFIED | Lines 251-256: `HighlightedName` for name, `result.fullName` subtle below; `handleEntitySelect` (line 214): `path: /entity/${encodeURIComponent(result.fullName)}` |
| 12 | Endpoint results show leaf name + _api/ path, navigate to /_api/<path> | ✓ VERIFIED | Lines 283-287: `HighlightedName` for name, `result.path` below; `handleEndpointSelect` (line 227): `path: /${result.path}` |
| 13 | Endpoint results show function vs navProperty icon distinction + Root badge for isRoot | ✓ VERIFIED | Lines 272-280: function → ƒ icon (`bg-type-fn`), navProperty → NAV badge (`bg-type-nav`); Lines 289-293: Root badge when `result.isRoot` |
| 14 | App.tsx handles new SearchSelection type with entity/endpoint and pre-computed path | ✓ VERIFIED | `App.tsx` lines 43-61: `navigate(selection.path)` — direct navigation; kindMap maps entity→'root', endpoint→'function' for recently-visited tracking |

**Score:** 14/14 truths verified (automated)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/lib/metadata/api-tree-walk.ts` | BFS tree-walk algorithm | ✓ VERIFIED | 129 lines, exports `buildApiEndpointIndex`, per-path ancestor tracking + depth limit |
| `app/src/lib/metadata/search-index.ts` | Two-kind search index builder | ✓ VERIFIED | 83 lines, exports `initSearchIndex`, `getSearchIndex`, `buildSearchIndex`; uses `buildApiEndpointIndex` |
| `app/src/lib/metadata/types.ts` | SearchDocument with kind: 'entity' \| 'endpoint' | ✓ VERIFIED | 84 lines, `SearchDocument` with correct shape, `EndpointEntry` exported |
| `app/src/lib/metadata/boot.ts` | Boot sequence passing LookupMaps | ✓ VERIFIED | 81 lines, `hydrate()` calls `initSearchIndex(data, getLookupMaps()!)` |
| `app/src/lib/metadata/index.ts` | Re-exports for new types | ✓ VERIFIED | 27 lines, exports `EndpointEntry`, `SearchDocument`, `getSearchIndex` |
| `app/src/components/search/CommandPalette.tsx` | 2-group UI with collapsible groups | ✓ VERIFIED | 383 lines (>200 min), `SearchGroup` component, 2 groups, Show more, collapse, fixed navigation |
| `app/src/App.tsx` | Updated search selection handling | ✓ VERIFIED | 87 lines, `navigate(selection.path)`, entity/endpoint kind map |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `api-tree-walk.ts` | `lookup-maps.ts` | `entityByFullName.get` / `functionById.get` | ✓ WIRED | Destructures from LookupMaps (line 29), uses `.get()` on lines 63, 74, 93, 102, 119 |
| `search-index.ts` | `api-tree-walk.ts` | `buildApiEndpointIndex()` | ✓ WIRED | Imports on line 2, calls on line 41 with `(metadata, lookupMaps)` |
| `boot.ts` | `search-index.ts` | `initSearchIndex(data, getLookupMaps()!)` | ✓ WIRED | Imports on lines 4, 6; calls on line 71 |
| `CommandPalette.tsx` | `search-index.ts` | `getSearchIndex()` | ✓ WIRED | Imports from `@/lib/metadata` (line 11), calls on line 195 |
| `CommandPalette.tsx` | Navigation | Entity→`/entity/<fullName>`, Endpoint→`/_api/<path>` | ✓ WIRED | `onSelect()` calls with pre-computed paths (lines 213-219, 225-233) |
| `App.tsx` | `CommandPalette.tsx` | `onSelect` callback | ✓ WIRED | Renders `<CommandPalette onSelect={handleSelect}>` (line 80), `navigate(selection.path)` (line 45) |
| `search/index.ts` | `CommandPalette.tsx` | Barrel re-export | ✓ WIRED | Exports `CommandPalette` + `SearchSelection` type (lines 1-2) |

### Requirements Coverage

No specific requirements mapped to phase 07.1 in REQUIREMENTS.md.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | — | — | None found |

No TODO/FIXME/PLACEHOLDER comments. No stub implementations. The `return null` patterns in `CommandPalette.tsx` (lines 194, 196) are legitimate guard clauses in `useMemo` — not stubs.

**Dead code verification:** All old code confirmed removed — `buildEntityPathMap`, `resolveSearchResultPath`, `GroupedResults`, `groupResults`, `MAX_PER_GROUP`, `entityPathMapRef`, `KindIcon`, `ResultBreadcrumb` — zero occurrences found in codebase.

### Build Verification

| Check | Status |
|-------|--------|
| `npx tsc --noEmit` | ✓ PASSED (zero errors) |
| `npm run build` | ✓ PASSED (1886 modules, 1.77s) |

### Human Verification — Completed During Checkpoint

All 5 human verification items were tested and approved by the user during the plan 07.1-02 checkpoint:

| # | Test | Status |
|---|------|--------|
| 1 | Entity search + navigation (Ctrl+K → "Web" → click SP.Web → /entity/SP.Web) | ✓ Approved |
| 2 | Endpoint search + navigation ("GetById" → click → /_api/ path loads) | ✓ Approved |
| 3 | Two-group display + Show more/collapse (tested with "Get", verified state reset) | ✓ Approved |
| 4 | Keyboard navigation across groups (arrow keys, Enter selects) | ✓ Approved |
| 5 | Visual distinctions (ƒ icon, NAV badge, Root badge) | ✓ Approved |

### Gaps Summary

**No gaps found.** All 14 truths verified (automated). All 5 human tests passed (checkpoint approval). All 7 artifacts verified. All key links wired. Build passes. Dead code removed.

---

_Verified: 2026-02-14T18:30:00Z_
_Verifier: Claude (gsd-verifier)_
