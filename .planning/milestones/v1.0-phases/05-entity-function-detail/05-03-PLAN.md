---
phase: 05-entity-function-detail
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - app/src/pages/ExplorePage.tsx
  - app/src/pages/TypesPage.tsx
autonomous: false

must_haves:
  truths:
    - "Navigating to an entity via nav property in Explore API shows full EntityDetail in content area"
    - "Navigating to /#/entity/SP.List shows full EntityDetail for SP.List"
    - "Clicking an entity type link in EntityDetail navigates to /#/entity/{typeName} and shows that entity's detail"
    - "Function detail in ExplorePage uses TypeLink component for consistent type rendering"
    - "EntityDetail works identically in both Explore API and Explore Types contexts"
    - "Entity not found shows appropriate error state"
    - "Visual styling matches mockup: table styling, colors, spacing, badges"
  artifacts:
    - path: "app/src/pages/ExplorePage.tsx"
      provides: "EntityDetail wired into content area for entity navigation"
    - path: "app/src/pages/TypesPage.tsx"
      provides: "EntityDetail wired for /#/entity/:typeName routes"
  key_links:
    - from: "app/src/pages/ExplorePage.tsx"
      to: "app/src/components/entity/EntityDetail.tsx"
      via: "import and render in content area"
      pattern: "EntityDetail"
    - from: "app/src/pages/TypesPage.tsx"
      to: "app/src/components/entity/EntityDetail.tsx"
      via: "import and render for /#/entity/:typeName"
      pattern: "EntityDetail"
    - from: "app/src/pages/TypesPage.tsx"
      to: "app/src/lib/metadata/lookup-maps.ts"
      via: "useLookupMaps to resolve typeName to EntityType"
      pattern: "useLookupMaps.*entityByFullName"
---

<objective>
Wire the EntityDetail component into both ExplorePage (content area for nav property navigation) and TypesPage (for `/#/entity/:typeName` routes). Refactor ExplorePage function detail to use TypeLink for consistency. Verify the complete visual experience.

Purpose: This connects the standalone EntityDetail (from Plan 01) to the app's navigation, completing all 14 requirements for Phase 5. The checkpoint ensures visual quality matches the mockup.
Output: Fully functional entity and function detail across both Explore API and Explore Types contexts.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-entity-function-detail/05-CONTEXT.md
@.planning/phases/05-entity-function-detail/05-01-SUMMARY.md
@.planning/phases/05-entity-function-detail/05-02-SUMMARY.md

## Mockup Reference
@.planning/phases/1-rebuild-ui/mockups/types-view.html

## Key Source Files
@app/src/pages/ExplorePage.tsx
@app/src/pages/TypesPage.tsx
@app/src/components/entity/index.ts
@app/src/routes.tsx
@app/src/hooks/use-api-navigation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire EntityDetail into ExplorePage and TypesPage</name>
  <files>
    app/src/pages/ExplorePage.tsx
    app/src/pages/TypesPage.tsx
  </files>
  <action>
    **ExplorePage — wire EntityDetail into content area:**

    Import `EntityDetail` and `TypeLink` from `@/components/entity`.

    In the content area's conditional rendering:
    - `currentFunction` branch: Refactor the parameter and return type rendering to use `TypeLink` component instead of inline type display. Import TypeLink from `@/components/entity`. Replace the parameter type `<span className="text-type-entity">` with `<TypeLink typeName={p.typeName} />`. Replace return type display with `<TypeLink typeName={currentFunction.returnType} />`. Keep the COMPOSABLE badge, void handling, and this-filtering from Plan 02.
    - `currentEntity` (no function) branch: Replace the minimal entity name/fullName display with `<EntityDetail entity={currentEntity} />`. This is shown when user navigates via a nav property (e.g., clicking "Lists" nav property shows the SP.List entity detail).
    - Keep all other branches unchanged (isRoot welcome, not-found).

    **TypesPage — wire EntityDetail for /#/entity/:typeName:**

    Import `EntityDetail` from `@/components/entity`, `useLookupMaps` from `@/lib/metadata`, and `useParams` from `react-router`.

    Replace the placeholder in TypesPage:
    - `useParams()` to get `typeName` param.
    - If no `typeName`: show welcome screen (from Plan 02, already implemented).
    - If `typeName` present:
      - Use `useLookupMaps()` to get `entityByFullName` map.
      - Look up entity: `entityByFullName.get(typeName)`.
      - If found: render `<EntityDetail entity={entity} />` in a scrollable content area with same padding as mockup (`p-6`).
      - If not found: show error state — "Entity not found" with the typeName displayed. Per Claude's discretion: centered message with muted text.
      - If maps not loaded yet: show loading state or nothing (metadata boot handles this).

    **No sidebar** in TypesPage (v2 deferred per user decision). The page layout is simply the content area filling the available space below the header.
  </action>
  <verify>
    `npm run build` in app/ directory succeeds. Test flows:
    1. Navigate to `/#/_api/web` → click "Lists" nav property in sidebar → content area shows full EntityDetail for SP.List with properties, nav properties, methods tables.
    2. Navigate to `/#/entity/SP.List` → shows full EntityDetail for SP.List.
    3. In any EntityDetail, click a type link (e.g., SP.ChangeToken in Properties) → navigates to `/#/entity/SP.ChangeToken` and shows that entity's detail.
    4. Navigate to `/#/entity/NonExistent` → shows "Entity not found".
    5. Function detail still works: `/#/_api/web/GetChanges` shows function with TypeLink-rendered params and return type.
  </verify>
  <done>
    EntityDetail renders in ExplorePage content area when navigating via nav properties. EntityDetail renders in TypesPage for `/#/entity/:typeName` routes. Function detail in ExplorePage uses TypeLink for consistent type rendering. Entity not found shows error state. All type links chain correctly between entities.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification</name>
  <action>
    Verify the complete entity and function detail system across both Explore API and Explore Types:
    - EntityDetail with collapsible Properties, Navigation Properties, and Methods sections
    - TypeLink with Collection split-link, primitive handling, and hover tooltips
    - Base type chain with inheritance walk
    - "Used by" cross-reference bar with purple chips
    - Section jump links and inline filters
    - Enhanced function detail with clickable type links
    - Explore Types welcome screen with real stats
    - Full routing: entity type links navigate to /#/entity/{typeName}
  </action>
  <verify>
    Start the dev server: `npm run dev` in app/ directory.

    **1. Entity Detail in Explore API:**
    - Navigate to `/#/_api/web` → click "Lists" in sidebar
    - Verify: SP.List entity detail shows with header "SP.List" + "Entity Type" badge
    - Verify: Base type chain shows "SP.SecurableObject → SP.ClientObject"
    - Verify: "Used by" purple bar shows chips like "SP.Web .Lists"
    - Verify: Section jump links show Properties (91), Navigation Properties (18), Methods (102)
    - Verify: Properties section expanded with table, inline filter works
    - Verify: Navigation properties in purple names, target types as green links
    - Verify: Methods section with blue method names, parameters one-per-line, COMPOSABLE badges

    **2. Type Link behavior:**
    - In Properties table, click "SP.ChangeToken" → navigates to `/#/entity/SP.ChangeToken`
    - In Nav Properties, note Collection types like "Collection(SP.ContentType)" — "Collection" portion dimmed, "SP.ContentType" portion green
    - Click the inner type of a Collection link → navigates to that entity
    - Verify Edm.* types (Edm.String, Edm.Int32) are gray/muted, NOT clickable

    **3. Function Detail:**
    - Navigate to `/#/_api/web/GetChanges` → function detail with green type links in params and return type
    - Navigate to a void-returning function → "void" in italic muted
    - Navigate to a function with no params → "none" in italic muted

    **4. Explore Types:**
    - Navigate to `/#/entity` → welcome screen with "Explore Types" title, three stat counters, hint
    - Navigate to `/#/entity/SP.List` → full EntityDetail (same as in Explore API)
    - Navigate to `/#/entity/SP.DoesNotExist` → "Entity not found"

    **5. Collapsible sections:**
    - Find an entity with 0 methods or 0 nav properties → section should be collapsed with (0) badge
    - Click the collapsed section → expands showing italic "No methods" / "No properties" message
    - Section jump link for (0) section should be dimmed

    **6. Dark mode:**
    - Toggle dark mode → all entity detail elements should have proper contrast

    **7. Inline filters:**
    - In Properties section, type "title" → only matching properties shown, count updates
    - In Methods section, type "get" → only matching methods shown
  </verify>
  <done>
    Human approves visual quality and functional behavior of entity detail, function detail, type links, Explore Types page, collapsible sections, inline filters, and dark mode.
  </done>
</task>

</tasks>

<verification>
- All 14 Phase 5 requirements (ENTD-01 through ENTD-11, FUNC-01 through FUNC-03) are satisfied
- EntityDetail works identically in both /#/_api/* and /#/entity/* contexts
- Type links chain correctly: clicking a type anywhere navigates to /#/entity/{typeName}
- Collection split-links render correctly with dimmed Collection + prominent inner type
- Tables match mockup styling
- Dark mode works throughout
- No regressions in existing navigation, sidebar, or breadcrumb behavior
</verification>

<success_criteria>
1. EntityDetail shows in ExplorePage content area when navigating via nav properties
2. EntityDetail shows in TypesPage for /#/entity/:typeName routes
3. Entity type links chain correctly through multiple navigation steps
4. Collection split-links work (dimmed Collection, green inner type)
5. Function detail uses TypeLink for consistent rendering
6. Explore Types welcome screen shows real stats
7. Entity not found shows appropriate error
8. Human verification passes for visual quality and interaction flows
</success_criteria>

<output>
After completion, create `.planning/phases/05-entity-function-detail/05-03-SUMMARY.md`
</output>
