---
phase: 04-explore-api-views
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/src/pages/HomePage.tsx
  - app/src/pages/ExplorePage.tsx
  - app/src/pages/index.ts
  - app/src/routes.tsx
  - app/src/components/layout/Header.tsx
autonomous: false

must_haves:
  truths:
    - "User lands on home screen and sees hero section with title, description, disabled search bar, and real metadata stats"
    - "User sees recently visited cards below a 'Browse all root endpoints' button"
    - "User clicks 'Browse all root endpoints' button and navigates to /#/_api with sidebar listing all 793 endpoints"
    - "User clicks a recently visited card and navigates to that endpoint"
    - "User can clear recently visited history"
    - "Empty recently visited state shows helpful message"
    - "Content area at /_api root shows welcome message about all root endpoints"
    - "Hero search bar shows 'Coming soon' disabled state"
  artifacts:
    - path: "app/src/pages/HomePage.tsx"
      provides: "Home screen with hero, browse-all button, recently visited grid"
      min_lines: 80
    - path: "app/src/routes.tsx"
      provides: "Separate routes: / -> HomePage, /_api/* -> ExplorePage"
      contains: "HomePage"
  key_links:
    - from: "app/src/pages/HomePage.tsx"
      to: "app/src/hooks/use-recently-visited.ts"
      via: "useRecentlyVisited() for items and clearAll"
      pattern: "useRecentlyVisited"
    - from: "app/src/pages/HomePage.tsx"
      to: "/#/_api"
      via: "navigate('/_api') on browse-all button click"
      pattern: "navigate.*/_api"
    - from: "app/src/pages/HomePage.tsx"
      to: "app/src/lib/metadata/metadata-store.ts"
      via: "useMetadataSnapshot() for real stats (function/entity/navprop counts)"
      pattern: "useMetadataSnapshot"
    - from: "app/src/pages/HomePage.tsx"
      to: "recently visited endpoint"
      via: "navigate(item.path) on recently visited card click"
      pattern: "navigate.*item\\.path"
---

<objective>
Build the home screen page and wire up routing, search UI placeholders, and the /_api root welcome message.

Purpose: This is the user-facing discovery experience — the first thing users see when opening the app. It provides a landing page for API exploration with recently visited history and a path into the full endpoint browser.
Output: Complete home screen, updated routing, search UI placeholder behavior, /_api welcome message.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-explore-api-views/04-CONTEXT.md
@.planning/phases/04-explore-api-views/04-01-SUMMARY.md

Source files:
@app/src/pages/ExplorePage.tsx
@app/src/pages/index.ts
@app/src/routes.tsx
@app/src/components/layout/Header.tsx
@app/src/hooks/use-recently-visited.ts
@app/src/hooks/use-api-navigation.ts
@app/src/lib/metadata/metadata-store.ts
@app/src/lib/metadata/types.ts
@.planning/phases/1-rebuild-ui/mockups/home-screen.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home screen page, routing split, and search UI placeholders</name>
  <files>app/src/pages/HomePage.tsx, app/src/pages/ExplorePage.tsx, app/src/pages/index.ts, app/src/routes.tsx, app/src/components/layout/Header.tsx</files>
  <action>
**Create `app/src/pages/HomePage.tsx`:**

A full home screen component matching CONTEXT.md decisions and following the mockup's visual language. NO sidebar layout — full-width centered content (like the mockup's State 1).

**Hero section** (centered, top of page):
- `<h1>` title: "SharePoint REST API Explorer"
- `<p>` description: "Browse, search, and understand every endpoint in the SharePoint REST API."
- Disabled search bar: visible but non-functional. Use a styled input with search icon (lucide `Search`), placeholder "Search functions, entities, properties...", disabled state. Add a small "Coming soon" text badge/label next to or overlaying the input. Style with reduced opacity (`opacity-60 cursor-not-allowed`) matching Header.tsx's existing disabled search style.
- Stats row below search: three items with colored dots showing real counts from metadata:
  - Blue dot + "{N} functions" — count from `Object.keys(metadata.functions).length`
  - Green dot + "{N} entities" — count from `Object.keys(metadata.entities).length`
  - Purple dot + "{N} nav properties" — sum all `entity.navigationProperties.length` across entities
  - Use `useMetadataSnapshot()` to get real metadata. Show "..." or skeleton while loading (metadata can be null).
  - Dot colors: `bg-type-fn` (blue), `bg-type-entity` (green), `bg-type-nav` (purple)

**"Browse all root endpoints" button** (positioned above recently visited per CONTEXT.md):
- Styled as a subtle bordered link/button: dashed border, hover turns blue (like mockup's `.all-endpoints-link`)
- Text: "Browse all root endpoints" with a count badge showing "793" (or actual count from `Object.values(metadata.functions).filter(f => f.isRoot).length`)
- Arrow icon on the right (→)
- Clicking navigates to `/_api` via `useNavigate()` from react-router

**Recently Visited section:**
- Section header: clock icon (lucide `Clock`) + "Recently Visited" title (uppercase, small, muted) + "Clear" action button on the right
- Grid of cards: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-2.5` (responsive, up to 3 columns, max 12 items = 4 rows)
- Each card (per CONTEXT.md):
  - Colored type icon on left: determines by `kind` from RecentlyVisitedItem:
    - `function` or `root` → blue `ƒ` icon, `bg-type-fn/10` background, `text-type-fn` text
    - `navProperty` → green `T` icon, `bg-type-entity/10` background, `text-type-entity` text (navProperties lead to entities, so entity-green is correct per CONTEXT.md's "green for entities")
  - Icon container: 32x32px, rounded-md, colored background
  - Name (font-semibold, truncated with `truncate` class + `title` attribute for full name on hover — EXPL-05)
  - Path in monospace (text-xs text-muted-foreground font-mono, truncated with `truncate` class + `title` attribute for full path on hover)
  - Relative time on right ("2h ago", "1d ago", etc.)
  - Clicking navigates to the item's path via `navigate(item.path)`
- Card styling: border, rounded-lg, padding, hover effect (shadow, border-color change)
- "Clear" button calls `clearAll()` from `useRecentlyVisited()`
- **Empty state** (per CONTEXT.md): "No recently visited endpoints. Browse all root endpoints to get started." with the "Browse all root endpoints" as a link to `/_api`

**Relative time helper** (inline or small util):
- Simple function: < 1 min → "just now", < 60 min → "Xm ago", < 24h → "Xh ago", < 7d → "Xd ago", else → "Xw ago"
- No external library needed

**Layout:**
- Full-width, centered content: `max-w-4xl mx-auto px-6 py-12`
- No sidebar, no breadcrumb — this is the home screen
- Follows mockup layout flow: hero → browse-all button → recently visited

**Update `app/src/routes.tsx`:**
- Import `HomePage` from `@/pages`
- Change the index route from `{ index: true, element: <ExplorePage /> }` to `{ index: true, element: <HomePage /> }`
- Keep `{ path: '_api/*', element: <ExplorePage /> }` as-is
- This means: `/` → HomePage, `/_api/*` → ExplorePage

**Update `app/src/pages/index.ts`:**
- Add `export { HomePage } from './HomePage'`

**Update `app/src/pages/ExplorePage.tsx`:**
- The `isRoot` state now means user is at `/_api` (not `/`). This is already correct since the route is `_api/*` and the hook checks for empty splat.
- Update the isRoot content area from the current placeholder ("Select an endpoint from the sidebar to explore") to a proper welcome message per CONTEXT.md:
  - `<h2>` "All Root Endpoints"
  - `<p>` describing the 793 root endpoints available, that users can filter in the sidebar or hover to see full names or drag sidebar to widen
  - A hint box (subtle blue background): "Click any endpoint in the sidebar to explore its entity, methods, and navigation properties."
  - Match the mockup's `.root-welcome` styling but using Tailwind classes

**Update `app/src/components/layout/Header.tsx`:**
Per CONTEXT.md search UI decisions:
- On home page (`/`): hero search is shown (in HomePage), so HIDE the header search input entirely
- On non-home pages: header search input is visible but disabled (already the case)
- Use `useLocation()` from react-router to detect if on home page (`pathname === '/'`)
- When on home: hide the center search div (render null or hidden)
- When on non-home: show existing disabled search input as-is
  </action>
  <verify>
Run `npm run build` from app/ — no errors. Navigate to `/#/` — see home screen with hero, browse-all button, recently visited section. Navigate to `/#/_api` — see welcome message in content area. Check Header search visibility toggles between home and non-home pages.
  </verify>
  <done>
Home screen displays at `/` with hero (title, description, disabled search, real stats), browse-all button, and recently visited grid. `/_api` root shows welcome message. Header search hidden on home, visible (disabled) elsewhere. Routing correctly splits `/` → HomePage and `/_api/*` → ExplorePage.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of home screen and browse experience</name>
  <files>none (verification only)</files>
  <action>
Verify the complete Phase 4 implementation:
- Home screen at `/#/` with hero section, stats from real metadata, disabled "Coming soon" search, browse-all button, recently visited cards
- Sidebar filter at all navigation levels with real-time filtering and count
- Root endpoints at `/#/_api` with green `<>` icon badges and welcome message
- Header search hidden on home, visible (disabled) on other pages
- Recently visited tracking in localStorage

Steps:
1. Open `http://localhost:5173/#/` — verify home screen layout:
   - Hero: title, description, disabled search bar with "Coming soon", real stats with colored dots
   - "Browse all root endpoints" button with count
   - Recently visited section (may be empty initially — check empty state message)
2. Click "Browse all root endpoints" — verify navigates to `/#/_api`:
   - Sidebar shows all root endpoints with green `<>` icon
   - Content area shows "All Root Endpoints" welcome message
   - Filter input visible at top of sidebar
3. Type in sidebar filter (e.g. "web") — verify list narrows and count updates
4. Click a root endpoint (e.g. "web") — verify FN/NAV tags appear, filter still visible
5. Go back to home `/#/` — verify recently visited shows the endpoint visited
6. Click "Clear" — cards disappear, empty state shows
7. Verify header search is hidden on home page, visible (disabled) on `/#/_api`
8. Check dark mode — toggle and verify home screen looks correct in both themes
  </action>
  <verify>Human visual verification of all items above</verify>
  <done>All visual checks pass: home screen layout correct, browse-all navigates properly, sidebar filter works at all levels, root items have green icons, recently visited tracks and displays, search UI placeholders behave correctly, dark mode looks good</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. Home screen displays at `/` with all required sections
3. Browse-all navigates to `/_api` with welcome message
4. Sidebar filter works at all levels
5. Root items have green `<>` icons
6. Recently visited tracks and displays correctly
7. Search UI placeholder behavior correct
8. Dark mode compatible
</verification>

<success_criteria>
- User lands on home screen with hero, stats, browse-all button, and recently visited
- Clicking browse-all navigates to /_api with sidebar filter and root endpoint list
- Filter narrows endpoints in real-time with count
- Recently visited cards show with clear functionality
- Root endpoints display green `<>` icon, deeper levels show FN/NAV tags
- Hero search disabled with "Coming soon", header search hidden on home
</success_criteria>

<output>
After completion, create `.planning/phases/04-explore-api-views/04-02-SUMMARY.md`
</output>
