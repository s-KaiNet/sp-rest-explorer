---
phase: 17-move-icons-in-search-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/search/CommandPalette.tsx
autonomous: true
requirements:
  - SMOD-01

must_haves:
  truths:
    - "Navigate hint stays on the left side of the search modal footer"
    - "Open and Close hints appear on the right side of the search modal footer"
    - "All three hints (Navigate, Open, Close) remain visible and styled identically to before"
  artifacts:
    - path: "app/src/components/search/CommandPalette.tsx"
      provides: "Rearranged footer hint bar layout"
      contains: "justify-between"
  key_links:
    - from: "footer div"
      to: "Navigate (left) vs Open+Close (right)"
      via: "flex justify-between with two child groups"
      pattern: "justify-between"
---

<objective>
Rearrange the search modal footer hint bar so that "Navigate" stays on the left side and "Open" + "Close" move to the right side.

Purpose: Improve visual separation of hint types — navigation vs. actions — by splitting the footer bar into left and right groups.
Output: Updated CommandPalette.tsx with split footer layout.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/components/search/CommandPalette.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split footer hint bar into left and right groups</name>
  <files>app/src/components/search/CommandPalette.tsx</files>
  <action>
In the footer hint bar section (the `div` after the `CommandList` closing tag, around line 477), change the layout from a single flat row of hints to a split layout with Navigate on the left and Open + Close on the right.

Current structure:
```
<div className="flex items-center gap-3 border-t ...">
  <span>Navigate</span>
  <span>Open</span>
  <span>Close</span>
</div>
```

Target structure:
```
<div className="flex items-center justify-between border-t border-border dark:border-modal-border px-3 py-2 text-xs text-muted-foreground">
  {/* Left group: Navigate */}
  <div className="flex items-center gap-3">
    <span className="flex items-center gap-1">
      <kbd ...>↑↓</kbd>
      <span>Navigate</span>
    </span>
  </div>
  {/* Right group: Open + Close */}
  <div className="flex items-center gap-3">
    <span className="flex items-center gap-1">
      <kbd ...>↵</kbd>
      <span>Open</span>
    </span>
    <span className="flex items-center gap-1">
      <kbd ...>Esc</kbd>
      <span>Close</span>
    </span>
  </div>
</div>
```

Keep all existing classes on the `kbd` elements and `span` elements exactly as they are. The ONLY change is:
1. Add `justify-between` to the outer `div` and remove `gap-3` from it (gap is now on inner groups)
2. Wrap the Navigate hint in a left `div`
3. Wrap Open + Close hints in a right `div` with `gap-3`
  </action>
  <verify>
Run `npm run build` from the `app/` directory to confirm no compilation errors. Visually verify by running `npm run dev` — the search modal footer should show Navigate on the left and Open + Close on the right.
  </verify>
  <done>
The search modal footer bar displays "Navigate" on the left side and "Open" + "Close" on the right side, with all kbd styling preserved.
  </done>
</task>

</tasks>

<verification>
1. Open the app and trigger Cmd+K / Ctrl+K to open the search modal
2. Look at the footer bar at the bottom of the modal
3. Confirm "↑↓ Navigate" is on the left
4. Confirm "↵ Open" and "Esc Close" are on the right
5. Confirm all keyboard hint badges have the same styling as before
</verification>

<success_criteria>
- Navigate hint is on the left side of the footer
- Open and Close hints are on the right side of the footer
- No visual regression in kbd badge styling
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-move-icons-in-search-modal/17-01-SUMMARY.md`
</output>
