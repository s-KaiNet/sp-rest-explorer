---
phase: 16-change-color-for-entity-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/entity/TypeLink.tsx
autonomous: true
requirements:
  - LINK-01
  - LINK-02

must_haves:
  truths:
    - "Entity type links (SP.Alert, SP.Group etc.) render in orange/amber entity color, not emerald green"
    - "Collection(Entity) links show muted 'Collection(' wrapper with orange/amber inner type name"
    - "Primitive types (Edm.*) and unknown types remain unchanged (gray muted-foreground)"
  artifacts:
    - path: "app/src/components/entity/TypeLink.tsx"
      provides: "Entity type link component with type-entity color"
      contains: "text-type-entity"
  key_links:
    - from: "app/src/components/entity/TypeLink.tsx"
      to: "app/src/index.css"
      via: "CSS custom property --type-entity through Tailwind text-type-entity"
      pattern: "text-type-entity"
---

<objective>
Replace hardcoded emerald green link color in TypeLink with the `--type-entity` CSS variable color (orange/amber) so entity type links match the TypeIcon entity color used throughout the app.

Purpose: Visual consistency — entity links should be the same orange/amber as entity icons, not a different green that conflicts with the root type color.
Output: Updated TypeLink.tsx using `text-type-entity` Tailwind class instead of `text-emerald-600 dark:text-emerald-400`.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/components/entity/TypeLink.tsx
@app/src/components/ui/type-icon.tsx
@app/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace emerald link color with type-entity color in TypeLink</name>
  <files>app/src/components/entity/TypeLink.tsx</files>
  <action>
In `TypeLink.tsx`, replace all occurrences of the hardcoded emerald color classes with the CSS variable-based entity color:

1. **Line 66** (Collection inner type link): Replace `text-emerald-600 hover:underline dark:text-emerald-400` with `text-type-entity hover:underline`
   - The `text-type-entity` class uses `--color-type-entity` which maps to `--type-entity` CSS variable
   - This automatically handles light/dark mode via the CSS variable definitions in index.css

2. **Line 89** (Direct entity/complex type link): Replace `text-emerald-600 hover:underline dark:text-emerald-400` with `text-type-entity hover:underline`

3. **Update the JSDoc comment** (line 23-24): Change "emerald clickable link" to "orange/amber clickable link" to reflect the new color.

Do NOT change:
- The `text-muted-foreground` on Collection wrapper text and parentheses — these stay muted
- The `text-muted-foreground` on primitive (Edm.*) types — these stay gray
- The `text-muted-foreground italic` on unknown/empty types — these stay as-is
- The `font-mono` or `font-medium` classes — keep these
  </action>
  <verify>
1. Run `npm run build` — no TypeScript or build errors
2. Visually confirm: grep TypeLink.tsx for "emerald" — should return zero matches
3. Visually confirm: grep TypeLink.tsx for "text-type-entity" — should return 2 matches (Collection inner + direct entity link)
  </verify>
  <done>
- TypeLink renders entity/complex type names in `--type-entity` color (orange/amber in light, lighter orange in dark)
- Collection(Entity) shows muted "Collection(" + orange entity name + muted ")"
- No emerald-600/emerald-400 references remain in TypeLink.tsx
- Build passes cleanly
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. No `emerald` color references in TypeLink.tsx
3. `text-type-entity` class present on entity link elements
4. Light mode: entity links render in orange/amber (oklch 0.55 0.15 80)
5. Dark mode: entity links render in lighter orange/amber (oklch 0.75 0.12 80)
</verification>

<success_criteria>
- All entity type links throughout the app (property tables, nav property tables, method tables, base type chains, detail views) render in the --type-entity color
- Collection wrapper text remains muted; inner type name uses entity color
- Light and dark mode both show correct entity color via CSS variables
- No hardcoded emerald color classes remain in TypeLink.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/16-change-color-for-entity-links/16-01-SUMMARY.md`
</output>
